<?xml version="1.0" encoding="utf-8"?>
<!--
  ★ 明文 HTTP 全局放开(2026-09-20 起)。

  以前只对本机回环开:那时 Java 层自己出网的只有本地数据通道,Emby 等请求走 Go 核心层,
  不经过 Android 的 NetworkSecurityPolicy。插件系统之后多了两条 Java 层出网:
  插件用的 WebView(嗅探 / 验证页,D59)和 :spider 进程里的 TVBox jar(D349)。
  它们访问的是用户订阅里的任意站点,大多是 http,域名没法事先列出来 ——
  不放开的表现是嗅探页一片空白、jar 下载失败,而且一句错都没有(实测)。
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
