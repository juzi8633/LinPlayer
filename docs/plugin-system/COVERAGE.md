# 决定落点核对表

> 由 `tools/coverage.py` 生成,不要手改。每条决定都必须在 SPEC 正文有落点;
> 需要接口的决定在 `api/` 里有 `@see Dnnn`;没有接口的写明为什么不需要。

- 决定总数:587
- SPEC 有落点:587
- API 有落点:334
- 结论:全部通过

| # | 决定(摘要) | SPEC 章 | API 落点 / 不需要 API 的原因 |
|---|---|---|---|
| D1 | 作者群体:社区开发者(写代码)+ 普通用户(填规则,不写代码)+ 搬运现有生态 | 01 | —(范围/原则,无接口) |
| D2 | 搬运现有生态只做 TVBox 系;不做 Stremio | 01 | —(范围/原则,无接口) |
| D3 | 插件语言:JS/TS | 01 | —(范围/原则,无接口) |
| D4 | UI 自由度上限:插件可以整页接管、可以改全局主题 | 01 | —(范围/原则,无接口) |
| D5 | 信任模型:完全信任 —— 装了即全权限,不做权限弹窗与沙箱拦截。例外只有这几处:拿不到 Emby 凭据(D11,含 D3… | 01 03 05 11 | —(范围/原则,无接口) |
| D6 | UI:插件作者能自己写组件、改写页面;官方同时提供一批现成组件,可在官方页面基础上改 | 01 | —(范围/原则,无接口) |
| D7 | TVBox 兼容:type 0/1 苹果CMS 接口 + JS spider(drpy 系)+ 整份配置 JSON 导入… | 17 | —(官方插件内部实现) |
| D8 | UI、数据源之外还开放:播放器(mpv 属性/命令/事件)、扩展现有功能(弹幕/字幕/元数据/排行榜的提供者;下载见 D… | 01 | —(范围/原则,无接口) |
| D9 | 分发四种都要:订阅仓库地址、官方市场、本地文件/扫码、开发模式热重载 | 14 | —(插件页与市场 UI) |
| D10 | 数据源在界面上当成一个"服务器",与 Emby 服务器并列,有自己的首页/分类/搜索。不进 Emby 详情页当备选线路;… | 01 08 | manifest.schema.json plugin-sdk.d.ts |
| D11 | 敏感数据:插件经宿主 API 能读媒体库/观看记录,拿不到 Emby token 等原始凭据(D5 完全信任的唯一例外) | 03 | plugin-sdk.d.ts |
| D12 | 防崩三件套:单次调用超时中断 + 连续报错自动禁用 + 启动连续崩溃 2 次后下次进安全模式(不加载任何插件,只是全关,… | 01 04 20 | —(范围/原则,无接口) |
| D13 | 开发者工具链:TS 类型定义包 + 脚手架 CLI(create/build/pack/dev;publish 只打包,… | 16 | —(工具链与文档) |
| D14 | 播放页:视频层不动,插件可整套替换控制栏(OSD)、加画面覆盖层 | 01 06 09 | manifest.schema.json plugin-sdk.d.ts |
| D15 | 接管冲突:同一个接管位(首页、播放控制栏、主题……)同时只生效一个,设置里让用户选 | 01 06 | —(范围/原则,无接口) |
| D16 | 插件可以后台运行(2026-09-20 推翻原「不能后台」):形态与限制见 D502~D505;没声明后台能力的插件仍只… | 05 12 | plugin-sdk.d.ts |
| D17 | 插件之间不能直接 import / 调用。共享代码靠打包时各自打进去;需要互通的数据走宿主注册表中转(D237 D273… | 05 17 | plugin-sdk.d.ts |
| D18 | JS 运行时 goja + esbuild(实测依据见 docs/research/plugins-v2/08-runt… | 02 | —(宿主架构内部实现) |
| D19 | 开放自绘(Canvas:线、形状、文字、动画),两端各实现一套绘图指令 | 07 | plugin-sdk.d.ts |
| D20 | 插件数据源"服务器"享受官方的观看记录/续播、收藏、弹幕匹配;不接官方下载器 | 08 | plugin-sdk.d.ts |
| D21 | 主题插件可改全部设计 token + 官方组件的样式定义(组件长什么样),组件的行为与结构不变 | 07 11 | plugin-sdk.d.ts theme-json.schema.json |
| D22 | 普通用户的"规则"直接用 drpy 规则格式,不另造格式 | 01 | —(范围/原则,无接口) |
| D23 | 插件 UI 写法:JSX + Preact,SDK 自带自定义渲染器,组件树序列化后交给两端原生渲染 | 01 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D24 | TVBox 兼容范围扩到:parses 解析接口 + WebView 嗅探取真实地址 + lives 直播源(m3u/t… | 01 17 | —(范围/原则,无接口) |
| D25 | 插件可以当下载后端(对接 aria2、网盘离线下载等);但数据源插件里的片不进官方下载器(与 D20 一致) | 01 08 10 | plugin-sdk.d.ts |
| D26 | 插件包 = 单个 zip(扩展名 .lpplugin):manifest.json + 打包好的 main.js + 图… | 04 | manifest.schema.json |
| D27 | 不做签名、不做哈希校验,下载即装 | 01 | —(范围/原则,无接口) |
| D28 | 官方市场 = 一个公开 GitHub 仓库:index.json 做索引,作者提 PR 上架,插件包放 Releases… | 14 15 | index.schema.json |
| D29 | 整页接管范围:任何页面(首页、详情页、播放控制栏、媒体库、搜索、设置……),例外:插件管理页(启用/禁用/卸载)与凭据相… | 01 03 06 14 | manifest.schema.json plugin-sdk.d.ts |
| D30 | 注入位:侧栏入口、首页栏目、详情页区块、右键/更多菜单 | 06 | manifest.schema.json |
| D31 | TV 焦点:可点组件默认自动可聚焦、方向键交给 Compose 原生;插件可覆盖(方向键目标、初始焦点、焦点组) | 07 | plugin-sdk.d.ts |
| D32 | 播放器全开放:读属性 + 监听事件、控制播放、任意 mpv 属性/命令透传、插件自带着色器/滤镜。例外:会泄露流地址的属… | 01 03 09 | plugin-sdk.d.ts |
| D33 | 插件停用/卸载时,宿主自动还原它改过的 mpv 属性,并撤掉它挂的着色器/滤镜(宿主记账,不靠插件自觉) | 04 09 | plugin-sdk.d.ts |
| D34 | 贡献点在 manifest 里声明、代码里实现(VS Code 模式):不执行代码就能列出冲突选择与设置项,用到时才懒加… | 01 04 06 | manifest.schema.json plugin-sdk.d.ts |
| D35 | 插件设置:manifest 声明设置项(开关/文本/下拉/数字),宿主自动生成设置页;需要复杂设置的插件可换成自己的 J… | 04 05 | manifest.schema.json |
| D36 | 兼容策略:manifest 写 minAppVersion,不承诺 API 兼容,破坏性变更由作者跟进 | 01 04 | manifest.schema.json plugin-sdk.d.ts |
| D37 | 播放地址两条路:① play() 返回 {url, headers},宿主设给 mpv(m3u8 子请求也要带头时走宿主… | 05 08 | plugin-sdk.d.ts |
| D38 | 插件存储:每插件独立 KV + 私有文件目录 + 密钥区。卸载时弹窗勾选「同时删除数据」(默认勾上),不删则重装后数据还… | 04 05 13 | plugin-sdk.d.ts |
| D39 | 插件界面不做多语言,插件写什么显示什么 | 01 04 | manifest.schema.json plugin-sdk.d.ts |
| D40 | 数据源"服务器"的页面官方画,插件只实现 home/category/search/detail/play 等动词给数据… | 08 | plugin-sdk.d.ts |
| D41 | 组件开放范围:官方页面用到的全部组件都开放给插件(原语 + 业务组件) | 07 | plugin-sdk.d.ts |
| D42 | 开发模式两种:① 桌面端指向本地目录,改文件自动重载(应用内 esbuild 编 TS);② CLI lp dev 局域… | 02 04 16 | —(宿主架构内部实现) |
| D43 | 直播源全套:频道列表 + 分组、播放中换台、EPG 节目单(XMLTV / DIYP)、回看/时移(catchup) | 17 | plugin-sdk.d.ts |
| D44 | 官方页面继续用 Avalonia/Compose 原生写;每个官方组件同时登记进插件组件表(镜像开放)。插件"在官方基础… | 01 07 | plugin-sdk.d.ts |
| D45 | 数据源在「添加服务器」页里和 Emby 并列添加:可选已装的数据源插件,或贴 TVBox 配置地址 —— 一个配置拆出多… | 06 08 17 | plugin-sdk.d.ts |
| D46 | TVBox 配置是订阅:记住地址,按用户设定的间隔拉最新(默认:每次启动拉一次 + 运行中每 24 小时),源的增删跟着… | 17 20 | —(官方插件内部实现) |
| D47 | 插件 id = 作者/名字(如 alice/douban-rating),与 GitHub 托管天然对应 | 01 04 | manifest.schema.json plugin-sdk.d.ts |
| D48 | 同 id 多来源:记住安装来源,只从装的那个来源更新;别处的同 id 显示"已安装(来自其它仓库)",要换得先卸载 | 04 14 | —(宿主生命周期行为) |
| D49 | 插件更新:一个全局「自动更新」开关,默认关;关着时仍在启动时检查,有新版在插件管理页亮红点,用户点才装 | 04 14 | —(宿主生命周期行为) |
| D50 | 更新时保留上一版包;新版连崩被自动禁用时可一键回退 | 04 | —(宿主生命周期行为) |
| D51 | 加载/卸载时机由插件在 manifest 里自己声明(启动即加载 / 用到才加载;常驻 / 闲置回收),宿主照办。未声明… | 02 04 | manifest.schema.json plugin-sdk.d.ts |
| D52 | 并发模型:每插件一个运行时 + 一个事件循环(单线程 + 异步),耗时 API 返回 Promise;同步请求只在子运行… | 01 02 | plugin-sdk.d.ts |
| D53 | 防崩阈值:UI 渲染/事件回调 1 秒、数据源类调用 30 秒,只计 JS 占用 CPU 的时间,不计网络等待;同一插件… | 01 02 04 05 06 07 08 20 | plugin-sdk.d.ts ui-protocol.d.ts |
| D54 | 线路:详情页顶部一排线路标签,切换后选集格跟着变;播放页内也能切线路,保持当前集数与进度 | 08 | plugin-sdk.d.ts |
| D55 | 数据源观看记录的键:源 + vod_id 精确匹配为主,对不上时按 片名 + 年份 + 集序号 找回(防资源站换域名/改… | 08 | plugin-sdk.d.ts |
| D56 | 弹幕匹配:按片名 + 集数自动匹配,错了用户手动纠正,纠正结果按整部剧记住 | 10 | plugin-sdk.d.ts |
| D57 | 分类筛选复用 Emby 媒体库的筛选面板;插件返回"有哪些维度、每个维度哪些选项" | 08 | plugin-sdk.d.ts |
| D58 | 多个解析接口(parses)按顺序试,失败换下一个 | 17 | —(官方插件内部实现) |
| D59 | WebView 嗅探:先开隐藏 WebView 加载页面、拦截请求抓 m3u8/mp4;15 秒抓不到就显示出来,让用户… | 05 17 19 20 | plugin-sdk.d.ts |
| D60 | Cookie:每个数据源/插件一个 Cookie 罐,WebView 与 req/fetch 共用(过一次盾后续请求都带… | 05 | plugin-sdk.d.ts |
| D61 | 直播频道多地址:播不了或卡顿超过 8 秒自动切下一个;播放页可手选「源 1/2/3」;记住每个频道上次能用的地址 | 17 20 | —(官方插件内部实现) |
| D62 | WebView 能力对所有插件开放(不只 TVBox):打开页面抓视频地址、在页面里执行 JS 取值、过盾后拿 Cook… | 05 | plugin-sdk.d.ts |
| D63 | 插件网络请求的 UA:插件自己设;没设时宿主给一个常见浏览器 UA(不发 UA 会吃 403)。插件请求是 UA 三道里… | 05 | plugin-sdk.d.ts |
| D64 | 插件可注册快捷键/遥控器按键,可以覆盖官方按键。插件按键与官方按键一起列在快捷键设置里,用户可改可清空;两个插件抢同一键… | 01 06 12 14 | manifest.schema.json plugin-sdk.d.ts |
| D65 | 播放页覆盖层默认点击穿透(只显示);声明"可交互"时才拦截点击与遥控器焦点 | 06 09 | examples/player-panel/src/main.tsx manifest.schema.json |
| D66 | 弹幕:插件可提供弹幕源、代发弹幕;不能替换官方弹幕渲染,也不做弹幕过滤插件(过滤宿主自己就有) | 01 10 | plugin-sdk.d.ts |
| D67 | 插件接管播放控制栏时,官方把字幕/音轨/倍速/画面增强等菜单做成可调用的子面板,插件 OSD 放个按钮即可打开;没放的用… | 09 | plugin-sdk.d.ts |
| D68 | 第三方站点登录(如代发弹幕要登 B 站)插件自己做:用 WebView(D62)或自绘表单,凭据存密钥区;官方不做统一账… | 10 | plugin-sdk.d.ts |
| D69 | 切换主题重启生效。连带:开发模式热重载(D42)对主题不生效,改主题要重启看效果 | 04 06 11 | —(宿主生命周期行为) |
| D70 | 主题可只提供浅色或深色一种;只有深色的主题在系统切到浅色时保持深色 | 11 | theme-json.schema.json |
| D71 | 主题可把字体文件打进 .lpplugin 包 | 04 11 | theme-json.schema.json |
| D72 | 桌面 / 手机 / TV 三套主题 token 分开做、分开适配;主题按形态分别提供,没提供某形态的主题在该形态上不可选… | 11 | manifest.schema.json |
| D73 | 字幕提供者:字幕菜单「在线搜索」列出各插件结果供手选;用户可开「没有字幕时自动挂最佳匹配」 | 10 | plugin-sdk.d.ts |
| D74 | 元数据提供者可以补充展示、覆盖 Emby 元数据(只在本地显示,不写回 Emby)、补全数据源元数据;是补充/覆盖/补全… | 10 | plugin-sdk.d.ts |
| D75 | 排行榜页是接管位:官方内置榜(D361)是默认项,插件可整页替换(多个时用户选一个,D15) | 06 18 | manifest.schema.json |
| D76 | 元数据冲突:设置里拖动排序元数据插件,靠前的覆盖靠后的,按字段生效(A 没给简介就用 B 的) | 01 10 | plugin-sdk.d.ts |
| D77 | 下载后端:点下载时选「用哪个下载」(Emby 片除外,D190),默认官方,可设默认后端;插件任务也进官方下载列表统一显… | 10 | plugin-sdk.d.ts |
| D78 | 外部下载(aria2/网盘)完成后,插件可返回一个可播 URL,进官方「已下载」列表直接播 | 10 | plugin-sdk.d.ts |
| D79 | 市场插件详情展示:截图、更新日志、下载量(GitHub Releases 计数)、贡献点清单(从 manifest 自动… | 06 14 | index.schema.json |
| D80 | 调试面板只在设置里打开开发者模式后出现 | 16 | plugin-sdk.d.ts |
| D81 | 调试面板四块:日志(按 debug / info / warn / error 分级筛选)+ 报错栈(esbuild s… | 04 05 16 | plugin-sdk.d.ts |
| D82 | lp publish 只打包出 .lpplugin,不自动上传、不自动提 PR | 16 | —(工具链与文档) |
| D83 | 上架流程:作者 Fork 官方市场仓库/自己的仓库托管自己的插件;要进官方索引时提 PR,维护者人工审核合并 | 15 | —(官方仓库、CI 与 Pages 站) |
| D84 | 导航:插件页与官方页共用一个导航栈(nav.push(页名, 参数)),返回键/遥控器 Back 统一官方处理;插件也能… | 05 06 07 | manifest.schema.json plugin-sdk.d.ts |
| D85 | 插件可拦截返回键,只限插件自己的页面(如「表单未保存,确定离开?」) | 05 07 | plugin-sdk.d.ts |
| D86 | 插件可用官方反馈组件:Toast、对话框(确认/输入/选择)、系统通知(应用运行时随时可发;后台发通知限 D504 的用… | 05 07 12 | plugin-sdk.d.ts |
| D87 | 插件的网络图片走官方图片缓存(内存 + 磁盘),可带请求头(Referer/Cookie),可用该源的 Cookie 罐… | 02 | plugin-sdk.d.ts |
| D88 | 动画:属性变化时声明 transition(时长/曲线),由原生端跑,不过 JS 桥;官方组件自带动效原样保留;逐帧动画… | 07 | plugin-sdk.d.ts |
| D89 | 插件样式可写任意值,官方圆角/间距刻度只约束官方代码;插件也能引用 token(如 radius.card)跟随主题 | 07 20 | plugin-sdk.d.ts |
| D90 | 样式写法:RN 式 style 对象(如 {padding: 10, radius: 'token:card'}),属性… | 07 | plugin-sdk.d.ts |
| D91 | 布局:Flex 子集(行/列 + 对齐 + 权重 + 间距 + 叠层),映射到 Avalonia StackPanel/… | 07 | plugin-sdk.d.ts |
| D92 | 插件可读:媒体库查询(封装好的有限接口:search / getItem / getChildren / getLate… | 01 03 05 08 | plugin-sdk.d.ts |
| D93 | 插件可写:标记已看/收藏(宿主代发到 Emby)、观看进度(本地与服务器)、应用设置。账号 / 服务器 / 网络代理类设… | 03 05 | plugin-sdk.d.ts |
| D94 | 插件可订阅事件:播放生命周期(开播/暂停/播完/切集)、导航(进了哪页/哪部片详情)、服务器切换、应用前后台 | 05 09 | plugin-sdk.d.ts |
| D95 | 插件拿不到 Emby 片的流地址(地址里带 api_key = 给 token);要播就调 player.play(it… | 03 09 | plugin-sdk.d.ts |
| D96 | 插件 fetch 跟随应用代理设置,可对单个请求声明直连 | 05 | plugin-sdk.d.ts |
| D97 | fetch 能力"能支持的都支持":流式读取、二进制(ArrayBuffer)、WebSocket、按请求声明忽略证书错… | 05 | plugin-sdk.d.ts |
| D98 | HTML 解析宿主提供三种(均 Go 实现):drpy 选择器 pdfh/pdfa、CSS 选择器 DOM API(qu… | 05 | plugin-sdk.d.ts |
| D99 | 加解密/编码宿主提供常用的(Go 实现):AES/DES/RSA/MD5/SHA/HMAC/Base64/GBK;Cry… | 05 | plugin-sdk.d.ts |
| D100 | TVBox 配置格式能支持的都支持,目标是"用户复制链接过来就能用":图片尾部藏 JSON、base64 前缀、AES … | 17 | —(官方插件内部实现) |
| D101 | TVBox 的 rules(m3u8 去广告)与 ads(广告域名)要做:播放前按规则改写 m3u8 剔除广告分片(走宿… | 17 | —(官方插件内部实现) |
| D102 | 插件可注册「m3u8 过滤器」:任何数据源播放前都过一遍,去广告插件可独立存在 | 06 17 | manifest.schema.json plugin-sdk.d.ts |
| D103 | 不做 DoH,TVBox 配置里的 doh 字段忽略 | 01 17 | —(范围/原则,无接口) |
| D104 | Canvas API 仿 HTML5 Canvas 2D(fillRect / fillText / path / dr… | 05 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D105 | Canvas 可逐帧(requestAnimationFrame),宿主按设备能力限到 30/60 fps,超时降帧不卡… | 07 20 | plugin-sdk.d.ts |
| D106 | 插件可拿单帧截图像素(mpv screenshot-raw),不能流式逐帧取像素 | 01 09 | plugin-sdk.d.ts |
| D107 | 画面处理一律走 mpv 着色器(D32);覆盖层只画 UI,不碰视频像素 | 09 | manifest.schema.json |
| D108 | EPG 来源四路都要:m3u 头 x-tvg-url、TVBox lives 的 epg(DIYP)、用户手填 XMLT… | 10 17 | plugin-sdk.d.ts |
| D109 | 直播个人化:收藏频道(置顶分组)、最近观看、遥控器数字键换台、可设「启动直进直播」播上次频道 | 17 | —(官方插件内部实现) |
| D110 | 直播不当服务器:侧栏独立「直播」入口,所有直播源汇总在一个直播页,不随服务器切换(与 D10 数据源不同) | 17 | —(官方插件内部实现) |
| D111 | 回看/时移格式能支持的都支持:append / shift / default(${start} ${end} 等模板)… | 17 | plugin-sdk.d.ts |
| D112 | 直播源三种加法:直播页贴 m3u/txt 地址(或本地文件)、TVBox 配置自带 lives(随订阅同步,D46)、直… | 10 17 | plugin-sdk.d.ts |
| D113 | 同名频道按归一化名合并成一个,各源地址都进「源 1/2/3」(配合 D61 自动切源) | 17 | plugin-sdk.d.ts |
| D114 | 解析接口都支持(jar 类含神解析随 D348 的运行环境):type 0 走 WebView 嗅探(D59)、type… | 17 | —(官方插件内部实现) |
| D115 | 用户可在解析管理页增删、排序(即 D58 的尝试顺序)、测试解析接口 | 17 | —(官方插件内部实现) |
| D116 | 装了带接管位的插件,装完问一句「xx 要替换首页,现在启用吗?」;不启用就保留官方,以后在设置里切 | 04 06 | —(宿主生命周期行为) |
| D117 | 从本地文件/扫码/非官方仓库安装时,先显示贡献点清单 +「非官方来源」字样,点确认才装。只是告知,不是权限拦截(D5) | 03 04 06 12 13 14 | —(宿主强制的安全规则) |
| D118 | 官方市场就是一个预装、不可删的默认订阅,格式与用户订阅的仓库完全一样;浏览页合并展示所有订阅并标来源 | 14 | index.schema.json |
| D119 | 仓库索引格式统一为 index.json:仓库名 + 插件数组(id / 名称 / 版本 / 下载地址 / 截图 / 贡… | 14 | index.schema.json |
| D120 | 用户写的 drpy 规则算一个数据源,不算插件:在「添加服务器」里贴规则文本/地址就加上,不走市场、没有 manifes… | 01 08 17 | —(范围/原则,无接口) |
| D121 | 规则编辑器不内置,做成一个插件(按原则"不必要的不内置") | 17 | —(官方插件内部实现) |
| D122 | 数据源失效:打开时首页请求失败就在服务器列表标红「上次访问失败」;另有「一键检测全部源」按钮。不做启动时自动检测 | 08 | —(宿主数据源页面行为) |
| D123 | 服务器列表按来源分组:Emby 一组、每个 TVBox 订阅一组(可折叠)、插件源一组;可拖动排序、可隐藏 | 08 | plugin-sdk.d.ts |
| D124 | 以下改为官方插件(官方写、市场里装,不装就没有),宿主只留它们要用的通用能力:直播(D108~D113 整套)、TVBo… | 01 08 17 18 | —(范围/原则,无接口) |
| D125 | 现有功能再改为官方插件:字幕翻译、Trakt / Bangumi 同步(排行榜、付费追剧日历已改回宿主,D361)。留在… | 01 17 18 | —(范围/原则,无接口) |
| D126 | 删掉现有的「影视目录」页(Windows/Android 的 CatalogPage)连同核心层 source.Cata… | 08 18 | —(宿主数据源页面行为) |
| D127 | 宿主开放子运行时 API:js.bundle(入口, 解析器) + js.createContext(注入的全局),插件… | 01 02 05 | plugin-sdk.d.ts |
| D128 | 开发者模式打开时,debug API(读任意插件的日志/请求/UI 树/KV)对所有插件开放;没开就不存在。调试面板插件… | 05 16 | plugin-sdk.d.ts |
| D129 | 定时:运行时里有标准 setTimeout/setInterval;另可在 manifest 声明周期任务(「每 N 小… | 05 06 12 | manifest.schema.json |
| D130 | 启动页是一个接管位:设置里「启动时打开」可选 首页 / 上次页面 / 各插件声明的页(如「直播:上次频道」),用户选一个… | 06 17 | manifest.schema.json plugin-sdk.d.ts |
| D131 | 插件可注册服务器类型:manifest 声明名称、图标、表单字段(如「配置地址」);用户提交后插件返回一个或多个源(多个… | 06 08 | manifest.schema.json plugin-sdk.d.ts |
| D132 | 一个插件可提供任意个数据源,运行时动态增删(TVBox 订阅更新会增删源) | 05 08 | manifest.schema.json plugin-sdk.d.ts |
| D133 | 页面保活:在导航栈里的插件页不销毁(返回后原样),出栈才销毁,与官方页一致 | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D134 | 大列表:VirtualList / VirtualGrid,插件给 itemCount + renderItem(i) … | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D135 | 事件节流:滚动/拖动每帧最多一条;输入框默认非受控(原生自己维护文字,只在 change/submit 通知 JS),要… | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D136 | UI 报错:区块级错误边界,哪块崩了就在那块显示「xx 插件出错 [重试] [禁用]」,不影响其它区块;接管位崩了自动回… | 01 04 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D137 | 跨设备同步「装了哪些插件、从哪装」(启用状态与接管位选择不同步,各设备自己管,D527);另一台设备提示「要装这 N 个… | 13 | —(宿主存储/同步/备份行为) |
| D138 | manifest platforms 字段声明支持平台(windows / linux / android / andr… | 04 11 14 | manifest.schema.json plugin-sdk.d.ts |
| D139 | 备份导出只含本地文件装的插件包(没处重下);市场/仓库装的只记 id + 来源 + 版本,恢复时重新下载 | 13 | —(宿主存储/同步/备份行为) |
| D140 | 插件 KV 同步冲突:按键记修改时间,后写覆盖 | 05 13 | plugin-sdk.d.ts |
| D141 | 内存:goja 无法按运行时限内存,改用全局水位看门狗:进程 Go 堆超阈值(TV 512MB / 桌面 1.5GB,待… | 02 19 20 | —(宿主架构内部实现) |
| D142 | 插件包大小不限,市场与安装确认页显示大小 | 04 14 | index.schema.json |
| D143 | 插件存储不设配额;占用在插件详情里显示(管理页列表不显示,D221),可一键清理缓存 | 03 05 13 | plugin-sdk.d.ts |
| D144 | 插件文件目录分 data/ 与 cache/:cache 可一键清理、不进备份与同步;data 进备份与同步(D38) | 05 13 | plugin-sdk.d.ts |
| D145 | lp CLI 是 Go 单文件,复用宿主同一份 esbuild + goja,作者不装 Node 也能 create/b… | 04 16 | —(宿主生命周期行为) |
| D146 | 宿主 API 写在单一定义文件里,同时生成 Go 注册骨架与 .d.ts,门禁比对两边(沿用 COMMANDS.md 生… | 16 | plugin-sdk.d.ts |
| D147 | 类型包 @linplayer/plugin-sdk 发布到公共 JS 包仓库;插件生态统一用 pnpm(lp creat… | 16 | plugin-sdk.d.ts |
| D148 | 插件可用第三方 JS 库,esbuild 打包进 main.js;依赖 Node 内置模块(fs/net)或 DOM 的… | 02 | plugin-sdk.d.ts |
| D149 | 官方插件仓库 = 用户 GitHub 上现有的插件仓库,清空后整仓重建;官方插件源码 + 官方市场 index.json… | 15 19 | —(官方仓库、CI 与 Pages 站) |
| D150 | 开发者文档放在插件仓库的 README / docs/,不上官网 | 15 16 | —(官方仓库、CI 与 Pages 站) |
| D151 | 示例两类都要:每个扩展点一个几十行的最小示例 + 官方插件本身作为完整示例 | 15 16 | —(官方仓库、CI 与 Pages 站) |
| D152 | 旧插件系统现在就删(core/plugin、core/plugincmd、core/source/pluginsrc、W… | 02 14 | —(宿主架构内部实现) |
| D153 | 数据源开放键沿用 plugin:<插件id>/<源id> 形状,插件 id 换成 作者/名字(如 plugin:alic… | 01 02 08 | plugin-sdk.d.ts |
| D154 | 深链四种:linplayer://plugin/install?url=(打开安装确认,D117)、linplayer:… | 06 12 | manifest.schema.json plugin-sdk.d.ts |
| D155 | 详情页注入位:标题下方、播放按钮旁、简介下方、页尾;此外插件可自定义插入位置:官方页每个区块有稳定锚点名(如 detai… | 01 06 | examples/detail-anchor/src/main.tsx manifest.schema.json plugin-sdk.d.ts |
| D156 | 首页栏目:官方栏目与插件栏目在设置里一个列表,用户拖动排序、可隐藏;新装插件的栏目默认追加到末尾 | 06 | manifest.schema.json |
| D157 | 右键/更多菜单可注入到:影片/剧集卡片、单集、播放页更多菜单、服务器/源列表项 | 06 | manifest.schema.json plugin-sdk.d.ts |
| D158 | 侧栏入口可排序/隐藏/固定,插件入口超过 2 个的默认收进「更多」;用户可关掉收纳,全部平铺滚动 | 06 20 | manifest.schema.json |
| D159 | 插件可替换或隐藏官方页里的单个锚点区块;同一区块多个插件抢 = 接管位冲突,用户选(D15) | 01 06 | manifest.schema.json |
| D160 | 注入区块的 props 是当前条目的完整统一数据(同 D92 查询 API 的结构:标题/年份/外部 id(TMDB/I… | 07 | plugin-sdk.d.ts |
| D161 | 插件 OSD 拿状态:宿主在状态变化时推送(暂停/音轨/缓冲…);进度这类高频值用官方 ProgressBar 组件原生… | 07 09 20 | plugin-sdk.d.ts |
| D162 | OSD 显隐由宿主按官方规则管(鼠标静止隐藏、触摸唤出)并告知插件;插件可声明自己管(如常驻进度条) | 09 | manifest.schema.json plugin-sdk.d.ts |
| D163 | 进度条预览缩略图由官方 ProgressBar 自带(Emby trickplay/BIF);数据源插件可提供自己的预览… | 07 10 | plugin-sdk.d.ts |
| D164 | 字幕显示改动走 mpv sub-* 属性透传(D32);字幕翻译插件生成翻译字幕文件再 sub-add。不开放插件自己渲… | 01 09 | plugin-sdk.d.ts |
| D165 | 季与集:数据源接口可选返回季结构;不给就扁平显示成一个选集格,集多时按 1-50 / 51-100 分页 | 08 | plugin-sdk.d.ts |
| D166 | 数据源搜索:滚到底加载下一页;离开页面或改关键词时取消进行中的请求(与现有跨服请求生命周期一致) | 08 | plugin-sdk.d.ts |
| D167 | 数据源首页:顶部是源的推荐行(TVBox homeVod),下面是分类入口,与 Emby 首页「媒体库 + 最新」结构对… | 08 | plugin-sdk.d.ts |
| D168 | 演员页:数据源接口有可选的 person 动词,实现了则详情页演员可点,否则纯文字 | 08 | plugin-sdk.d.ts |
| D169 | 插件更新在更新页完成后,弹窗「是否重启软件立即生效」;不重启则下次启动生效 | 04 | —(宿主生命周期行为) |
| D170 | 启用/禁用不即时生效,可以批量改;用户离开插件管理页或退出程序时,弹窗「插件有改动,重启后生效」[立即重启 / 稍后],… | 04 14 | —(宿主生命周期行为) |
| D171 | 同一插件的多份配置(两台 aria2、两个账号)由插件自己在设置里管;宿主一个插件一个实例 | 10 | —(宿主对提供者结果的处理) |
| D172 | 插件出错时(错误边界、自动禁用提示)提供「复制错误详情」和「反馈」(打开 manifest 里的 issues 地址);… | 04 20 | manifest.schema.json plugin-sdk.d.ts |
| D173 | 数据源播放复用现有预取/多线程加载代理(直链走预取,m3u8 按现有规则);插件可对自己的源声明「禁用预取」(有的站多连… | 02 08 | manifest.schema.json plugin-sdk.d.ts |
| D174 | 数据源进度不进跨服续播,只在该源内有效(跨源片名匹配误判多) | 08 | —(宿主数据源页面行为) |
| D175 | 数据源的「继续观看」:出现在该源自己的首页顶部 + 官方全局「观看历史」页;不混进 Emby 服务器首页 | 08 | —(宿主数据源页面行为) |
| D176 | 资源站换域名:源自己处理(drpy hostJs 动态取域名、TVBox 订阅更新带新地址);同时用户可在服务器列表里手… | 08 | plugin-sdk.d.ts |
| D177 | Linux 的 WebView 用系统 WebKitGTK;没装时 WebView 能力返回「不可用」,插件自行降级,嗅… | 05 20 | plugin-sdk.d.ts |
| D178 | Windows 的 WebView 用系统自带的 WebView2(它就是 Windows 的系统 WebView:Wi… | 05 | plugin-sdk.d.ts |
| D179 | 能力探测:运行时 app.capabilities 查 webview / canvas / 着色器 / TV 模式 /… | 04 05 | manifest.schema.json plugin-sdk.d.ts |
| D180 | 插件版本号强制 semver,不合法的拒装;更新判断、回退、同 id 比较都靠它 | 03 04 | index.schema.json manifest.schema.json plugin-sdk.d.ts |
| D181 | 没有测试版通道;semver 预发布后缀(-beta)不做特殊处理 | 01 04 | —(范围/原则,无接口) |
| D182 | 更新日志:完整的 CHANGELOG.md 随包;index.json 带最新一版摘要,供更新弹窗直接显示(不用先下包) | 04 14 | index.schema.json |
| D183 | 图标/截图接受 PNG / WebP / SVG,图标建议 256×256,截图不限;宿主统一走图片缓存缩放 | 04 20 | manifest.schema.json |
| D184 | 多弹幕源(官方弹弹 Play + 插件源)合并渲染,弹幕设置里按源开关;合并时按「时间 + 文字」去重 | 10 | plugin-sdk.d.ts |
| D185 | 弹幕匹配:宿主给线索(片名、年份、季/集号、外部 id(TMDB/豆瓣/Bangumi)、文件名、时长、文件哈希(有时)… | 10 | plugin-sdk.d.ts |
| D186 | 弹幕源插件分批交付弹幕(按时间窗);批的切分按条数控制而不是固定时长,高潮段落弹幕暴增时自动切小窗口,防单批过大卡住 | 10 | plugin-sdk.d.ts |
| D187 | 字幕提供者拿到的线索 = 弹幕同一套(D185)+ 用户字幕语言偏好;插件返回候选(名称/语言/格式/评分),选中后插件… | 10 | plugin-sdk.d.ts |
| D188 | 插件下到的字幕存宿主字幕缓存,按条目记住「这集用了哪个在线字幕」,下次播放自动挂上 | 09 10 | plugin-sdk.d.ts |
| D189 | 下载后端接口:submit(地址, 请求头, 文件名) → 任务 id、status(id)(进度/速度/状态)、pau… | 10 | plugin-sdk.d.ts |
| D190 | Emby 的片只能用官方下载器;外部下载后端只用于非 Emby 来源(守 D95,不把带 token 的地址交给插件) | 03 10 | plugin-sdk.d.ts |
| D191 | 外部下载后端可下:数据源的片(「用 aria2/网盘离线下载」)+ 插件自己的链接(磁力等);只是不进官方下载器 | 10 | plugin-sdk.d.ts |
| D192 | 元数据提供者在打开详情页时按需调用,结果按条目缓存 7 天,详情页更多菜单可「刷新元数据」;列表/海报墙不逐项调 | 10 20 | plugin-sdk.d.ts |
| D193 | 元数据插件可提供搜索别名:把「中文名 → Emby 条目」映射存本地,搜索时一并匹配 | 10 | plugin-sdk.d.ts |
| D194 | 手机播放页手势当作一种「按键」:插件可注册/覆盖,用户可改绑,冲突用户选,停用自动归还(同 D64) | 06 09 12 | manifest.schema.json |
| D195 | 系统通知权限(Android 13+)由宿主在第一次有插件发通知时申请;被拒就降级成 Toast | 12 | plugin-sdk.d.ts |
| D196 | 插件可:用系统浏览器打开 URL、调起其它 App(深链/Intent)、写剪贴板、调系统分享 | 05 12 | plugin-sdk.d.ts |
| D197 | 插件可读写剪贴板(读用于「复制 TVBox 地址后打开应用自动识别」这类功能) | 05 12 | plugin-sdk.d.ts |
| D198 | 字幕翻译、Trakt/Bangumi 同步的宿主实现已删,等官方插件重做。排行榜、付费追剧日历当时一并删了,按 D361… | 18 | —(宿主内置功能) |
| D199 | OAuth:优化自建 OAuth 中转的网页,用户在我们的页面上登录 Trakt/Bangumi 等,走我们的 API … | 05 18 | plugin-sdk.d.ts |
| D200 | 付费追剧日历(宿主内置,D361)照旧走自建代理 verify 订单号,只在解锁时校验一次;仍是软锁 | 03 18 | —(宿主强制的安全规则) |
| D201 | 官方插件被卸载后入口直接消失,与第三方插件一致;市场里随时可重装 | 17 | —(官方插件内部实现) |
| D202 | 官方插件不预装(包里不带任何插件);任何地方都不做「官方推荐」位(像广告),官方插件和第三方一样在市场里找。按场景的提示… | 01 14 17 | —(范围/原则,无接口) |
| D203 | OAuth 登录两种都做:桌面/手机默认本机浏览器跳转 + linplayer:// 回调;TV 用设备码(二维码 + … | 05 18 | plugin-sdk.d.ts |
| D204 | 自建 OAuth 中转对所有插件开放(第三方插件也能用我们在 Trakt/Bangumi 申请的应用身份)。已知风险:被… | 03 05 18 | plugin-sdk.d.ts |
| D205 | 「官方插件」识别 = id 以官方作者名开头 且 安装来源是官方市场(D48);本地文件装的同 id 不算。明文 JS … | 03 17 | index.schema.json |
| D206 | 入口文件:export default definePlugin({ activate(ctx), deactivate… | 04 06 | manifest.schema.json plugin-sdk.d.ts |
| D207 | 宿主 API 用 import:import { player, nav, fetch, storage } from … | 02 05 16 | plugin-sdk.d.ts |
| D208 | fetch 兼容 Web 标准写法(返回 Response:.text() / .json() / .arrayBuff… | 05 | plugin-sdk.d.ts |
| D209 | 补齐常用 Web 全局:URL / URLSearchParams、TextEncoder / TextDecoder、… | 02 05 | plugin-sdk.d.ts |
| D210 | 主题分端:一个主题包只服务一个端的 UI,官方不做跨端映射;作者想覆盖多端就为每个端各发一个独立的包(桌面、手机、TV … | 01 06 11 | manifest.schema.json |
| D211 | 主题可按官方图标的稳定名替换任意图标(SVG),没给的用官方 | 11 | plugin-sdk.d.ts theme-json.schema.json |
| D212 | 主题可换全局背景:静态图和视频都可以 | 11 | plugin-sdk.d.ts theme-json.schema.json |
| D213 | 主题可完全自定义动效(任意关键帧动画),不限于调参数 | 11 | theme-json.schema.json |
| D214 | 桌面(Windows/Linux)主题 = Avalonia 原生 .axaml 样式文件,运行时加载,与官方 Them… | 03 04 11 | manifest.schema.json |
| D215 | Android 手机/TV 主题 = 我们定义的 JSON(tokens + 官方组件 × 状态的样式属性 + 关键帧动… | 04 11 | manifest.schema.json theme-json.schema.json |
| D216 | 手机主题与 TV 主题也是两个包(platforms: android / android_tv) | 11 | manifest.schema.json theme-json.schema.json |
| D217 | 响应式:useViewport() 给宽高与官方断点(compact / medium / expanded,与官方页同… | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D218 | 列表项要求 key(React 语义);缺了用下标,开发者模式下报 warn | 07 | plugin-sdk.d.ts |
| D219 | 插件页可设:页面标题、全屏/沉浸(隐藏侧栏与状态栏)、横竖屏锁定、屏幕常亮;离开页面自动恢复 | 07 | plugin-sdk.d.ts |
| D220 | 桌面端插件可开子窗口(如弹幕姬、独立节目单窗口);手机/TV 上降级为整页 | 06 07 12 | manifest.schema.json plugin-sdk.d.ts |
| D221 | 插件管理页列表每项:图标 / 名称 / 版本 / 作者 / 来源(+ 官方标记)、启用开关 + 状态(正常 / 已自动禁… | 04 14 | —(宿主生命周期行为) |
| D222 | 所有接管位的选择集中在插件管理页的「接管位」分区:每个接管位一个下拉(官方 / 插件 A / 插件 B…),含首页、播放… | 06 11 14 | —(宿主对贡献点的调度规则) |
| D223 | 插件管理页有「一键全部禁用」,记住之前的启用状态可一键恢复(按 D170 重启生效) | 14 | —(插件页与市场 UI) |
| D224 | 插件详情页:设置(D35)、贡献点清单、占用 + 清缓存/清数据(D143 D144)、更新日志 / 回退上一版(D50… | 05 13 14 | —(运行时内部行为) |
| D225 | 数据源在服务器列表里与 Emby 同一套卡片,右上角来源角标(Emby / TVBox / 插件名);图标可用服务器图标… | 08 | —(宿主数据源页面行为) |
| D226 | 当前激活的是数据源时,它没有的能力(媒体库筛选、演员页、官方下载器、服务器端收藏同步……)入口直接不显示,不做置灰按钮 | 08 | —(宿主数据源页面行为) |
| D227 | 老用户升级后首次启动弹一次「插件系统已重做:字幕翻译、Trakt/Bangumi 同步改为官方插件 [去安装]」,只对老… | 18 | —(宿主内置功能) |
| D228 | ~~付费追剧日历回到宿主后,老用户的已解锁状态沿用,不用重新输订单号~~ 被 D550 推翻(旧记录在已删的 WebVi… | 18 | —(宿主内置功能) |
| D229 | 版本:新插件系统上线时 VERSION 升 2.0.0;在此之前不发正式版,避免用户拿到「功能删了、插件还没有」的空窗版 | 18 19 | —(宿主内置功能) |
| D230 | 订阅更新删掉某个源时,该源的收藏/观看记录保留;用户换同订阅里的其它源继续看(具体交互见 D232) | 05 08 17 | plugin-sdk.d.ts |
| D231 | 订阅更新新增的源自动加入并显示 | 08 17 | plugin-sdk.d.ts |
| D232 | 换源:详情页/播放页有「换源」按钮,按片名搜同一部片,列出结果,选一个跳过去并尽量定位到同一集。源下线、播不了、清晰度差… | 08 | —(宿主数据源页面行为) |
| D233 | 换源分三层,默认顺序:① 当前源的其它线路(不用搜);② 标记了「允许聚合」的数据源的线路;③ 标记了「允许聚合」的 E… | 08 | —(宿主数据源页面行为) |
| D234 | 换源并发上限 8,单源超时 8 秒,结果边出边显示 | 02 08 20 | —(宿主架构内部实现) |
| D235 | 「允许聚合」一个开关管所有聚合:关了就不进聚合搜索、聚合视界、换源。Emby 服务器默认开(老行为不变);TVBox 源… | 08 17 | plugin-sdk.d.ts |
| D236 | 换源跳到别的源时带上播放进度(同一集从当前位置接着播,片头长度不同可能差几秒);这是用户主动换源,不违反 D174 | 08 | —(宿主数据源页面行为) |
| D237 | 插件间数据互通走宿主注册表中转:宿主定义「直播频道源」「EPG 源」这类扩展点(与数据源同一模式),TVBox 插件把配… | 01 05 10 | plugin-sdk.d.ts |
| D238 | TVBox 配置里的 rules/ads 由 TVBox 插件自己注册到宿主的 m3u8 过滤器扩展点(D102);独立… | 17 | —(官方插件内部实现) |
| D239 | 市场浏览页:搜索(名称/描述/作者)、分类(按贡献点从 manifest 自动推出:数据源 / 主题 / 播放器 / 弹… | 14 | index.schema.json |
| D240 | 不做评分/评论;展示作者仓库的 GitHub star 数当热度参考。star 数由官方市场仓库的 CI 定时抓取写进 … | 01 14 15 | index.schema.json |
| D241 | 多个订阅仓库里的同 id 插件合并成一条(显示最高版本,官方来源优先),展开可看各来源;已装的仍只认安装来源(D48) | 14 | —(插件页与市场 UI) |
| D242 | 官方搜索页里插件可加「搜索建议 / 快捷动作」(如「在豆瓣查看」「搜磁力」),结果列表本身不动 | 06 | manifest.schema.json plugin-sdk.d.ts |
| D243 | 做全局命令面板:桌面用快捷键调出一个输入框,输入 / 开头就列命令;手机(TV 同手机)在搜索页输入 / 进入命令模式。… | 06 12 | manifest.schema.json plugin-sdk.d.ts |
| D244 | 播放结束时宿主可问插件「下一个播什么」(随机播放、跨季连播、播放列表类插件);没有插件接这个位就走官方规则。多个插件抢 … | 01 06 08 09 | manifest.schema.json plugin-sdk.d.ts |
| D245 | 本地文件只能通过系统文件选择器:files.pick() / files.save(),插件拿到用户选的那一个文件的内容… | 05 | plugin-sdk.d.ts |
| D246 | 插件不能自己监听端口;要给播放器喂数据(改写 m3u8、解密分片)走宿主本地代理的插件路由(D37),宿主把请求转给插件… | 03 05 | plugin-sdk.d.ts |
| D247 | 局域网访问:manifest 声明「会访问局域网」,安装确认/启用时明确写出;没声明却访问局域网地址 → 宿主拦截并报错… | 03 04 05 14 20 | manifest.schema.json plugin-sdk.d.ts |
| D248 | 发弹幕用官方输入框(颜色/位置),发送时选目标站 = 开启了发送能力的弹幕源(插件实现 send);没有任何源支持发送时… | 10 | plugin-sdk.d.ts |
| D249 | 发出的弹幕立刻在本地显示并加高亮边框,不等服务器回包;发送失败 Toast 提示并标记该条 | 10 | plugin-sdk.d.ts |
| D250 | TV 上不能发弹幕,只看 | 10 | plugin-sdk.d.ts |
| D251 | 数据源首页/分类结果的缓存由插件自己做(KV / cache 目录),宿主不缓存 | 08 | plugin-sdk.d.ts |
| D252 | 宿主不对插件请求限频,防封由插件自己管 | 02 03 05 08 | plugin-sdk.d.ts |
| D253 | 插件抛带类型的错误(限流 / 需要验证 / 站点不可用 / 解析失败 / 未找到…),宿主按类型显示对应文案。「未找到」… | 02 08 20 | plugin-sdk.d.ts |
| D254 | 数据源各动词返回我们的统一结构(与 D92 同一套:标题/年份/海报/外部 id…);TVBox 插件在内部把 vod_… | 01 02 08 17 | plugin-sdk.d.ts |
| D255 | 列表类接口(分类/搜索/首页推荐)用不透明游标翻页:返回 {items, next?},next 是插件自定的字符串(可… | 02 08 | plugin-sdk.d.ts |
| D256 | play() 返回:地址 + 请求头(D37),另可选:外挂字幕(地址+语言,宿主 sub-add)、多清晰度(播放页可… | 08 | plugin-sdk.d.ts |
| D257 | 解析/嗅探由插件自己驱动:play() 必须返回最终可播地址,需要解析就在 play() 里自己调 fetch 与 We… | 08 17 | plugin-sdk.d.ts |
| D258 | 聚合搜索(搜索页)与换源(播放页)是两件事。聚合搜索每个源只展示前 8 条候选;搜索接口只有一个 search,不分快速… | 08 20 | plugin-sdk.d.ts |
| D259 | 换源结果宿主按相似度判定:片名完全一致 > 年份一致 > 集数接近 > 包含关键词;明显不对的(解说/预告/同名别作)折… | 08 | —(宿主数据源页面行为) |
| D260 | 数据源动词只有 detail + play 必须;home / category / search / person /… | 08 | plugin-sdk.d.ts |
| D261 | 换源排序:层级(D233)在最外;层内按相似度分三档「同一部 / 可能是 / 不像(折叠)」;档内按延迟,延迟 = 该源… | 08 | —(宿主数据源页面行为) |
| D262 | 聚合搜索结果按源分行(每行一个源,最多 8 张,D258),谁先返回谁先显示,与现有 Emby 跨服搜索一致 | 08 | —(宿主数据源页面行为) |
| D263 | 播放失败不自动切线路,直接弹换源列表让用户选(直播频道自动切源 D61 不受影响) | 08 | —(宿主数据源页面行为) |
| D264 | 源码根目录一个独立 manifest.json,原样打进 .lpplugin;SDK 发 JSON Schema 供编辑… | 04 16 | manifest.schema.json |
| D265 | id 规则:作者与名字都只许小写字母/数字/连字符。进官方索引时 CI 校验作者名 = PR 提交者的 GitHub 用… | 03 04 15 | manifest.schema.json plugin-sdk.d.ts |
| D266 | 官方插件作者名 = linplayer(如 linplayer/tvbox、linplayer/live);官方市场 C… | 04 15 17 | manifest.schema.json |
| D267 | 自动设置页(D35)的项:开关 / 文本 / 下拉 / 数字,另加:密码/密钥(圆点显示,值存密钥区,备份规则同 D49… | 04 05 13 | manifest.schema.json plugin-sdk.d.ts |
| D268 | ~~<Player>:全局只有一个 mpv 实例……桌面视频是独立子窗口,壳要做区域跟随~~ 前提已作废(D561):桌… | 02 07 19 | plugin-sdk.d.ts |
| D269 | 插件页可内嵌网页:<WebView> 组件(显示论坛/评论/登录页),插件可往里注 JS、收消息;WebView 不可用… | 07 | plugin-sdk.d.ts |
| D270 | 不做「整页用 HTML/CSS 写」的模式;UI 只有 JSX → 原生组件一条路(D23),真要网页就用 <WebVi… | 01 07 | plugin-sdk.d.ts |
| D271 | 插件页首帧出来前宿主显示官方通用骨架屏,首帧到了再换 | 07 | ui-protocol.d.ts |
| D272 | 注册表是推模式:注册者把数据交给宿主落盘(如 TVBox 插件拉完订阅 registry.put('live.chann… | 04 05 10 13 | plugin-sdk.d.ts |
| D273 | 注册表的通道宿主和插件都能定义:宿主定义的(直播频道源、EPG 源、m3u8 过滤器……)有固定格式并校验;插件可开以自… | 01 03 05 06 | manifest.schema.json plugin-sdk.d.ts |
| D274 | XMLTV/DIYP 节目单由直播插件自己在 JS 里解析,宿主不内置 EPG 解析。已知代价:goja 解几十 MB … | 10 17 | plugin-sdk.d.ts |
| D275 | 添加 TVBox 订阅时,若配置含直播源而没装直播插件,提示一次「这份配置包含 N 个直播源,安装官方直播插件?」[安装… | 05 06 17 | manifest.schema.json plugin-sdk.d.ts |
| D276 | <Player> 所在页被返回或跳走时停播并记住进度,与离开官方播放页一致;不做悬浮小窗 | 07 | plugin-sdk.d.ts |
| D277 | 插件可注册导航拦截器:看到目标页名 + 参数,可放行或改跳(如「打开动漫详情改跳番剧页」)。比整页接管(D29)更细 | 01 06 | manifest.schema.json plugin-sdk.d.ts |
| D278 | 插件可注册列表变换钩子:官方列表(首页各栏、媒体库、搜索结果……)渲染前过钩子,可增删改、重排 | 01 02 06 | manifest.schema.json plugin-sdk.d.ts |
| D279 | 插件可放全局悬浮层:盖在所有页面上方(含播放页),默认点击穿透,声明可交互才拦截点击与焦点(同 D65) | 06 09 | examples/player-panel/src/main.tsx manifest.schema.json plugin-sdk.d.ts |
| D280 | 多个导航拦截器/列表变换钩子的顺序由用户在设置里拖动排序(与 D76 同一种列表),默认按安装顺序。拦截器:第一个改跳的… | 01 06 | plugin-sdk.d.ts |
| D281 | 钩子/拦截器出错或超过 300ms 就跳过,当它不存在,原数据/原路由继续;计入 D53 连续报错自动禁用。写坏一个钩子… | 01 02 06 20 | plugin-sdk.d.ts |
| D282 | 列表变换钩子可插入别处的条目(如往 Emby「最新」混数据源的片):必须是统一结构(D254)并带来源,卡片显示来源角标… | 06 | plugin-sdk.d.ts |
| D283 | 多个全局悬浮层都显示,按安装顺序叠(后装的在上);用户可在插件详情里单独关掉某插件的悬浮层 | 06 14 | manifest.schema.json |
| D284 | 插件 KV API 同步:storage.get(k) 直接返回;写入内存立即生效,宿主后台批量落盘。钩子(D281)里… | 05 13 | plugin-sdk.d.ts |
| D285 | 没开开发者模式时,插件 console.* 每插件在内存保留最近 500 条(不写磁盘);「复制错误详情」(D172)连… | 04 20 | plugin-sdk.d.ts |
| D286 | 官方设置各页也有稳定锚点(复用 D155 机制),插件可往里插一节,标题标明来自哪个插件;插件自己的设置仍在插件详情(D… | 06 | examples/settings-section/src/main.ts manifest.schema.json plugin-sdk.d.ts |
| D287 | 服务器信息只给 id + 用户起的显示名 + 类型(id 跨设备稳定:Emby 用 ServerId + UserId … | 03 05 | plugin-sdk.d.ts |
| D288 | servers.current() 返回当前服务器 id + 类型,配合 D94 的切服事件;拦截器/钩子可按当前服务器… | 05 | plugin-sdk.d.ts |
| D289 | 插到官方设置页的一节(D286)两种写法都行:manifest 声明设置项(D267,零代码、样式与官方一致),或给一个… | 06 | manifest.schema.json |
| D290 | 进了官方市场的作者视为可信:发新版时作者直接提 PR 改自己的 index 条目,不做逐版代码审查(首次上架仍按 D83… | 03 15 | —(宿主强制的安全规则) |
| D291 | lp dev 局域网推送要手动打开:开发者模式里点「接受推送」,屏幕显示 6 位配对码,lp dev --device … | 03 16 20 | —(宿主强制的安全规则) |
| D292 | 开发版与正式版同 id 时开发版优先:开发期间正式版挂起、数据共用,列表标「开发版」;停止开发(断开推送/移除目录)后恢… | 04 16 | plugin-sdk.d.ts |
| D293 | lp create 只生成一个空白模板;各扩展点的写法看示例仓库(D151) | 16 | —(工具链与文档) |
| D294 | 已上架作者的更新 PR CI 校验通过就自动合并。条件:只改自己 id 的条目 + 提交者是该作者(D265)+ 包能下… | 03 15 | —(宿主强制的安全规则) |
| D295 | 插件包在合并时由 CI 复制进官方仓库 Releases(D28):作者删仓/换包不影响已上架版本,下载量在一处统计(D… | 15 | index.schema.json |
| D296 | 不做下架标记、远程禁用机制(官方市场只收可信作者,D290) | 01 15 | —(范围/原则,无接口) |
| D297 | OSD 贡献点声明适用范围:点播 / 直播 / 某个来源 / 自己的 <Player>;每种场景各是一个接管位,用户分别… | 06 09 17 | manifest.schema.json plugin-sdk.d.ts |
| D298 | 全局只有一条着色器链:官方「画面增强」、插件 A、插件 B 多选一;开一个时提示「开启后将关闭 xx」,用户确认才切 | 06 09 | manifest.schema.json plugin-sdk.d.ts |
| D299 | 插件改的 mpv 属性一直生效到插件停用,停用时按 D33 还原;不做按集自动还原 | 09 | plugin-sdk.d.ts |
| D300 | 播放页侧边面板(选集/字幕/音轨那栏)是注入位:插件可加标签页(图标 + 名字 + JSX 内容),如评论、剧情说明、节… | 06 09 | examples/player-panel/src/main.tsx manifest.schema.json plugin-sdk.d.ts |
| D301 | 同一 mpv 属性被多方改(插件之间、插件与用户设置)时后写生效;宿主只记「谁改过什么、改之前是多少」,开发者模式可看到… | 09 | plugin-sdk.d.ts |
| D302 | 插件停用时,它改过的每个属性还原到它改之前的值,不管之后有没有被别人改过(已知:可能冲掉用户后来的手动调整) | 03 09 | plugin-sdk.d.ts |
| D303 | 插件首页栏目两种写法:返回条目列表(统一结构,官方画成与「最新」一样的海报行,主题自动跟随),或给一个 JSX 区块自己… | 06 | manifest.schema.json plugin-sdk.d.ts |
| D304 | 插件可接四种外部输入,manifest 声明匹配规则(URL 模式 / 扩展名 / 文本正则):Android 分享目标… | 06 12 | manifest.schema.json plugin-sdk.d.ts |
| D305 | 插件可给自己的侧栏入口加角标(nav.setBadge(数字 / 红点 / 清除));入口收进「更多」时角标冒泡到「更多… | 05 06 | manifest.schema.json plugin-sdk.d.ts |
| D306 | 文件关联:安装包预先关联一批常见扩展名(m3u / m3u8 / txt 直播源 / torrent / 常见视频…,官… | 12 | manifest.schema.json |
| D307 | 剪贴板识别默认关,在设置里开;有插件声明剪贴板规则时,插件详情里提示可以打开(Android 12+ 每次读剪贴板系统会… | 12 | manifest.schema.json |
| D308 | lp CLI 两种分发:pnpm 包 @linplayer/cli(按平台装对应二进制,esbuild 同款做法,版本跟… | 15 16 | —(官方仓库、CI 与 Pages 站) |
| D309 | 插件可让官方下载器下 Emby 片:download.enqueue(itemId),宿主取流、插件拿不到地址(守 D9… | 03 05 09 20 | plugin-sdk.d.ts |
| D310 | 插件可播任意地址:player.playUrl(地址, 请求头, {标题, 海报, 记录键}),进官方播放页;给了记录键… | 03 09 | plugin-sdk.d.ts |
| D311 | 插件可触发投屏:cast.open() 弹官方设备选择,投当前正在播的;插件拿不到设备列表和投出去的地址 | 05 09 | plugin-sdk.d.ts |
| D312 | 落代码第一阶段:运行时(goja + esbuild)+ manifest + 安装管理 + 数据源接口 + 官方页面画… | 19 | —(实施计划与验收) |
| D313 | 2.0.0 首发必须有的官方插件:TVBox 兼容、直播、Trakt/Bangumi 同步、字幕翻译、调试面板、规则编辑… | 17 | —(官方插件内部实现) |
| D314 | 三端推进:每个阶段桌面先打通(有自检脚本),Android 手机/TV 紧跟;2.0.0 要求三端齐 | 19 | —(实施计划与验收) |
| D315 | TV 上输入框两种打字方式并存:系统输入法 + 手机扫码输入(复用宿主已有的手机扫码遥控,输入框旁显示二维码,手机上打字… | 02 07 12 | plugin-sdk.d.ts |
| D316 | TVBox 插件的 drpy 子运行时:每源一个、按需创建、闲置回收,同时最多保留 TV 4 个 / 桌面 16 个,最… | 02 05 17 20 | plugin-sdk.d.ts |
| D317 | 不提供插件测试工具(不做 lp test、不做数据源体检),作者靠开发模式热重载手测,或自选 JS 测试框架 | 01 16 | —(范围/原则,无接口) |
| D318 | 插件 UI 的变化按帧合批:一帧(16ms)内的所有变化合成一个 patch 发给原生端,原生端一帧只重排一次 | 02 07 20 | plugin-sdk.d.ts ui-protocol.d.ts |
| D319 | 老版宿主遇到未知组件画成「需要更新 LinPlayer」占位小块,未知属性直接忽略,开发者模式报 warn;页面其余部分… | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D320 | 官方页被整页接管后,命令面板(D243)里仍可「打开官方首页/详情页」;长期切回在接管位里选官方(D222) | 06 12 | —(宿主对贡献点的调度规则) |
| D321 | 第一阶段验收数据:仓库里放假站 fixture(苹果CMS 接口 + drpy 规则 + 加密配置样本)跑门禁;用户提供… | 19 | —(实施计划与验收) |
| D322 | 第一阶段验收四项全过才算完:① 全链路能播(添加订阅 → 选源 → 首页/分类/搜索/详情 → 出画面);② 订阅更新(… | 19 | —(实施计划与验收) |
| D323 | 四种错误类型宿主给动作按钮:需要验证 →[去验证] (开 WebView 过盾,Cookie 进该源罐子后自动重试);需… | 02 08 20 | plugin-sdk.d.ts |
| D324 | 旧插件系统留在用户磁盘上的数据(已装旧插件、其设置)在 2.0.0 首次启动时静默删除;升级弹窗(D227)里加一句「旧… | 13 18 | —(宿主存储/同步/备份行为) |
| D325 | 进备份与跨设备同步:TVBox 订阅 + 源列表设置(勾选、排序/隐藏、允许聚合、手改 host)、数据源收藏、数据源观… | 08 13 17 | plugin-sdk.d.ts |
| D326 | 数据源收藏跟当前服务器走(切到哪个源看哪个源的收藏,与 Emby 一致);全局「观看历史」旁加「全部收藏」汇总各源 | 08 18 | —(宿主数据源页面行为) |
| D327 | 阶段顺序:① 运行时 + 数据源 + TVBox(D312)→ ② UI 渲染器 → ③ Trakt·Bangumi 同… | 19 | —(实施计划与验收) |
| D328 | 开发期间每个阶段本地提交,到 2.0.0 一起推(push main 会触发自动预发布,D229);已知风险:推之前本机… | 19 | —(实施计划与验收) |
| D329 | SPEC 定稿后先出宿主 API 定义文件草稿(D146:manifest schema + 各扩展点签名 + 宿主 A… | 19 | —(实施计划与验收) |
| D330 | 统一结构有可选「备注」字段(TVBox vod_remarks:「更新至 12 集」「HD」「完结」),显示为海报角标,… | 08 17 | plugin-sdk.d.ts |
| D331 | 线路名要显示站点给用户看的名字(如「星空线路」「银河线路」),由 TVBox 插件负责抓到;实在取不到才退回内部代号(f… | 08 17 | plugin-sdk.d.ts |
| D332 | 选集格可正序/倒序切换,每部剧记住自己的顺序;Emby 剧集页同样适用 | 08 18 | —(宿主数据源页面行为) |
| D333 | 卸载数据源插件或删掉 TVBox 订阅后,它的收藏与观看记录永远保留(属于宿主数据,不受 D38 卸载勾选影响):观看历… | 04 08 13 | plugin-sdk.d.ts |
| D334 | 筛选维度由插件声明单选或多选(TVBox filters 全是单选,原样映射);面板与 Emby 筛选面板同一套 | 08 17 | plugin-sdk.d.ts |
| D335 | 筛选条件按「源 + 分类」记住;有筛选生效时分类标题旁显示小点 + 一键清除 | 08 | plugin-sdk.d.ts |
| D336 | 海报比例由插件按分类声明:竖版 2:3 / 横版 16:9 / 方形,宿主按比例换网格列数;不声明默认竖版 | 08 17 | plugin-sdk.d.ts |
| D337 | 数据源详情页没有横版背景图时用无背景布局(左海报右信息,类似 TVBox);有横图(源自带或元数据插件补到,D74)时用… | 08 | —(宿主数据源页面行为) |
| D338 | 数据源搜索停顿 500ms 自动搜(与 Emby 搜索一致);改关键词时取消进行中的请求(D166)。聚合搜索例外:按回… | 08 20 | —(宿主数据源页面行为) |
| D339 | 搜索历史全局一份(Emby 与数据源共用),最多 50 条,可单条删/清空,进备份与同步 | 08 13 18 20 | —(宿主数据源页面行为) |
| D340 | 源没实现 person(D168)时,演员/导演名点了在当前源搜这个名字;源也没实现 search 才是纯文字 | 08 | plugin-sdk.d.ts |
| D341 | 数据源条目没有海报或图挂了,显示统一灰色占位图,与 Emby 缺图一致 | 08 | —(宿主数据源页面行为) |
| D342 | 数据源片头片尾:播放页菜单「跳过片头 N 秒 / 片尾 N 秒」按整部剧记住;play() 返回了时间(D256)就用返… | 08 10 | plugin-sdk.d.ts |
| D343 | 再次打开同一部剧默认上次的线路(按剧记线路名),线路没了退回第一条 | 08 | —(宿主数据源页面行为) |
| D344 | 源没实现 home 时,首页直接显示第一个分类(顶部分类标签默认选中第一个);分类也没有就只有搜索框 | 08 | plugin-sdk.d.ts |
| D345 | 订阅刷新失败(网络不通、解密失败、地址失效)时沿用上次成功的副本,并弹 Toast 提示原因 | 17 | —(官方插件内部实现) |
| D346 | 多仓配置(urls / storeHouse)添加时列出各仓,勾选哪几个加;每个勾选的仓成为一个独立订阅(一个分组),再… | 17 | plugin-sdk.d.ts |
| D347 | 配置里的公告(notice)、壁纸(wallpaper)、logo 全部忽略 | 17 | —(官方插件内部实现) |
| D348 | 支持 jar(csp_)与 Python spider:公开配置实测 jar 源占比高(三份常见配置:33 个里 26 … | 05 17 | plugin-sdk.d.ts |
| D349 | Android:jar 用系统 DexClassLoader 原生加载,Kotlin 壳实现 catvod 的 Spid… | 05 | plugin-sdk.d.ts |
| D350 | 桌面:jar/py 运行环境(JVM + dex 转换 + 安卓 API 桩 + Python)做成宿主下载组件,与补帧… | 05 | plugin-sdk.d.ts |
| D351 | 桌面跑安卓 jar 没有先例,先做 spike 再定:用公开配置的 jar 源在桌面 JVM 上跑 home/searc… | 05 17 19 | plugin-sdk.d.ts |
| D352 | jar 按 md5 缓存,订阅刷新时检查:md5 没变用本地,变了重下,下载失败沿用旧 jar;没给 md5 按订阅刷新… | 05 17 | plugin-sdk.d.ts |
| D353 | jar 的 proxy:宿主本地代理兼容 TVBox 的 /proxy 入口,启动时优先占用 TVBox 约定的固定本地… | 03 05 | plugin-sdk.d.ts |
| D354 | jar 跑在独立子进程:Android 声明一个 :spider 进程跑所有 jar,跨进程调用;jar 崩了只重启子进… | 02 05 | plugin-sdk.d.ts |
| D355 | jar/py 源超时按墙钟 30 秒(含网络;goja 的 CPU 计时管不到子进程);超时或崩溃计入该源的错误数,连错… | 02 05 20 | plugin-sdk.d.ts |
| D356 | 直播插件的总原则:TVBox 怎么操作直播源,我们就怎么操作(按键、手势、菜单习惯照搬),下面几条是已确认的具体形态 | 17 | —(官方插件内部实现) |
| D357 | TV 直播:进来就全屏播上次频道;确认键从左侧滑出「分组 / 频道 / 节目单」三栏,左右键在栏间移动;上下键直接换台 | 17 | —(官方插件内部实现) |
| D358 | 桌面直播:左侧分组 + 频道,右侧 <Player>(D268)+ 下方节目单,点全屏进官方播放页;手机竖屏上方播放、下… | 17 | —(官方插件内部实现) |
| D359 | TV 遥控器「上」= 上一个频道,设置里可反转 | 17 | —(官方插件内部实现) |
| D360 | 回看/时移三种入口都做:节目单里点已播节目(显示「回看」标,从头播)、直播进度条往回拖(「回到直播」按钮跳回)、暂停后从… | 17 | —(官方插件内部实现) |
| D361 | 排行榜、付费追剧日历改回宿主内置(推翻 D125 对这两项的决定):它们要用弹弹 Play 签名与 TMDB key,做… | 01 03 18 | —(范围/原则,无接口) |
| D362 | 内置排行榜的榜单:弹弹 Play 动漫榜 + TMDB 影视榜(老功能)+ 新增 Bangumi 榜;插件可整页替换(D… | 18 | —(宿主内置功能) |
| D363 | Trakt/Bangumi 同步插件做全三层,只去掉「播完评分弹窗」:① 播放即 scrobble 进度、Bangumi… | 17 | —(官方插件内部实现) |
| D364 | 字幕翻译插件两种引擎都有:用户自填(OpenAI 兼容大模型接口 / DeepL 等翻译 API 的 key,存密钥区)… | 17 | —(官方插件内部实现) |
| D365 | Trakt/Bangumi 账号连接回宿主(登录、刷 token,走 D199 的 OAuth 中转):追剧日历直接用;… | 03 05 17 18 | plugin-sdk.d.ts |
| D366 | 追剧日历回宿主时顺手加两项:① 已在 Emby 媒体库里的集标「已入库 / 可播」,点了直接播;② 开播提醒:追的剧有新… | 18 | —(宿主内置功能) |
| D367 | 调试面板插件只有桌面(Windows/Linux,platforms 限定);手机、TV 不做。手机/TV 调试时,日志… | 16 17 | —(工具链与文档) |
| D368 | 规则编辑器插件只做源码编辑器 + 测试按钮(给懂 drpy 的人),不做表单向导 | 17 | —(官方插件内部实现) |
| D369 | 手机/TV 主题 JSON 预览:桌面开发者模式里有「手机/TV 主题预览」页,用官方组件样品即时渲染 JSON;真机上… | 11 16 | —(主题加载与宿主设置) |
| D370 | 市场与插件包下载直连优先,失败时自动套用户在设置里填的 GitHub 加速前缀重试;不内置任何镜像地址(守提交红线);跟… | 14 | —(插件页与市场 UI) |
| D371 | 市场拉取失败/离线时显示上次缓存的市场 + 横条「市场列表更新失败,显示的是 N 天前的」;已装插件照常用 | 14 | —(插件页与市场 UI) |
| D372 | 桌面 .axaml 主题加载失败时回退官方主题启动,Toast「xx 主题加载失败,已改用官方主题 [复制错误]」;计入… | 04 11 | —(宿主生命周期行为) |
| D373 | 截图对所有插件(含主题)都是可选的 | 04 | manifest.schema.json |
| D374 | 嗅探需要用户手动过验证时(D59),WebView 跳一个整页打开,返回后继续 | 05 17 | plugin-sdk.d.ts |
| D375 | Android 系统 WebView 缺失或过旧时,提供可下载的内核组件(GeckoView):下载到应用私有目录动态加… | 05 | plugin-sdk.d.ts |
| D376 | 安全模式(D12):启动后弹一次「上次启动连续崩溃,已关闭全部插件。最可疑:xx 插件(崩溃前最后在跑的)」[去插件页]… | 04 | —(宿主生命周期行为) |
| D377 | 解析管理两个入口:TVBox 插件设置里的「解析管理」页;播放页侧栏「解析」标签(D300),播需要解析的片时显示当前用… | 08 09 17 | plugin-sdk.d.ts |
| D378 | TVBox 插件设置项:订阅刷新间隔(默认 D46)、嗅探超时(默认 15 秒,D59)、启用配置自带去广告规则(默认开… | 17 | —(官方插件内部实现) |
| D379 | 手动刷新订阅:服务器列表订阅分组的菜单(D157)里「立即刷新 / 编辑地址 / 重新勾选源 / 删除订阅」;手机服务器… | 17 | —(官方插件内部实现) |
| D380 | 设置里统一「扩展组件」页管理所有宿主下载组件(补帧、jar 环境、Python、GeckoView……):是否已装、大小… | 01 02 09 18 | plugin-sdk.d.ts |
| D381 | 插件可在 manifest 声明「需要某宿主组件」:安装确认页列出「可能需要下载:xx(约 N MB)」;真正用到时宿主… | 04 05 14 18 20 | manifest.schema.json plugin-sdk.d.ts |
| D382 | 组件独立版本,应用启动时检查,有新版在扩展组件页亮红点、用户点才更新;应用升级需要新版组件时才强制提示 | 18 | plugin-sdk.d.ts |
| D383 | 服务器切换器:按分组折叠(同 D123,订阅组默认折叠)+ 收藏源(星标的单独一组置顶)+ 最近使用置顶(最近切过的 5… | 08 20 | —(宿主数据源页面行为) |
| D384 | 源健康度只显示失败标红(维持 D122),不显示耗时色标 | 08 | —(宿主数据源页面行为) |
| D385 | 一键检测全部源:并发 8,调 home(没有则首个分类)拿到列表即算活;不测播放地址 | 02 08 20 | —(宿主架构内部实现) |
| D386 | 弹幕设置里一个「弹幕源」列表:官方弹弹 Play + 各插件源并列,每个有开关(D184)与当前匹配结果/手动纠正入口;… | 10 | —(宿主对提供者结果的处理) |
| D387 | 字幕「在线搜索」结果按插件分组,谁先返回先显示;组内按插件给的评分与用户语言偏好排序 | 10 | —(宿主对提供者结果的处理) |
| D388 | 插件页入口:设置里一项 + 命令面板,不占侧栏;用户可自己固定到侧栏(D158) | 14 | —(插件页与市场 UI) |
| D389 | 插件页顶部四个标签:已安装(列表 D221 + 全部更新 + 一键全禁 D223)/ 市场(D239)/ 接管位(D22… | 14 | —(插件页与市场 UI) |
| D390 | 已安装列表:有问题的在前(已自动禁用 / 有更新 / 待重启),其余按名称排 | 14 | —(插件页与市场 UI) |
| D391 | 「全部更新」:并发下载,全部装完弹一次「是否重启立即生效」(D169);失败的单独列出 | 04 14 | —(宿主生命周期行为) |
| D392 | 插件仓库 GitHub Pages 站四块内容:插件浏览(搜索/分类/排序,同 D239)+ 每个插件的详情页(截图、贡… | 15 | —(官方仓库、CI 与 Pages 站) |
| D393 | Pages 站由 CI 静态生成:合并 index.json 后用 pnpm 构建,每个插件一个静态页(可被搜索引擎收录… | 15 | —(官方仓库、CI 与 Pages 站) |
| D394 | Pages 视觉与 LinPlayer 应用一脉:深色玻璃、沿用应用 token(圆角/间距刻度、Accent 色),丰… | 15 | —(官方仓库、CI 与 Pages 站) |
| D395 | 域名沿用插件仓库已配置的自定义域名:用 GitHub Actions 部署 Pages(自定义域名存在仓库 Pages … | 15 | —(官方仓库、CI 与 Pages 站) |
| D396 | Pages 首页第一屏直接是插件墙:顶部一行搜索 + 分类 chip,下面插件卡片网格;不做宣传大头图 | 15 | —(官方仓库、CI 与 Pages 站) |
| D397 | 插件卡片以截图为主的横卡:有截图铺满卡面(悬停轮播),没截图用图标放大 + 模糊底;下方名称 / 作者 / 分类 / 下… | 15 | —(官方仓库、CI 与 Pages 站) |
| D398 | Pages 动效四种都做:卡片悬停上浮 + 滚动进入错落淡入;列表→详情共享元素过渡(View Transitions … | 15 | —(官方仓库、CI 与 Pages 站) |
| D399 | Pages 站中英双语:站点框架文字与开发者文档都有中英两份 | 15 16 | —(官方仓库、CI 与 Pages 站) |
| D400 | manifest name / description 必填,另有可选 i18n.en.name / i18n.en.d… | 04 15 | index.schema.json manifest.schema.json |
| D401 | Pages 站浅色/深色都做,默认跟随系统,可手动切换;玻璃与光斑在浅色下另调一套 | 15 | —(官方仓库、CI 与 Pages 站) |
| D402 | 插件详情页另显示:README 渲染(CI 抓取存静态)、历史版本及 .lpplugin 直链(D295)、支持平台 /… | 14 15 | —(插件页与市场 UI) |
| D403 | 电脑看 Pages 装到其它设备:详情页二维码(手机扫码打开安装深链);手机上可选「装到已配对的 TV」,走现有扫码遥控… | 02 12 14 15 | —(宿主架构内部实现) |
| D404 | 点「安装」深链 2 秒没反应就显示「没有反应?」:下载 LinPlayer(GitHub Releases)+ 手动下载… | 15 20 | —(官方仓库、CI 与 Pages 站) |
| D405 | 开发者文档与插件墙在同一个 Astro 工程,文档部分用 Starlight(自带搜索、多语言、侧栏)换成我们的视觉;一… | 15 | —(官方仓库、CI 与 Pages 站) |
| D406 | Pages 站不做访问统计(不引第三方脚本) | 01 15 | —(范围/原则,无接口) |
| D407 | 凭据相关页永远官方:添加服务器 / 登录 / 账号设置 / 网络代理设置 —— 不可整页接管、不可被导航拦截器(D277… | 03 05 06 | manifest.schema.json plugin-sdk.d.ts |
| D408 | 官方市场上架须知(措辞以免责为主、不写得咄咄逼人):插件由作者独立开发维护、官方不对插件内容负责;希望作者不在界面中投放… | 04 14 15 | index.schema.json manifest.schema.json |
| D409 | 允许收费插件,官方不接支付、不抽成:作者自己做解锁(如订单号软锁),市场详情页标「含付费功能」 | 03 04 14 | index.schema.json manifest.schema.json |
| D410 | 官方插件仓库结构:plugins/<名字>/ 官方插件、packages/sdk 类型包、packages/cli CL… | 15 | —(官方仓库、CI 与 Pages 站) |
| D411 | SDK 版本号与 LinPlayer 应用版本一致(SDK 2.1.0 = 应用 2.1.0 的 API);lp bui… | 04 16 | manifest.schema.json plugin-sdk.d.ts |
| D412 | 官方插件各自独立发版:子目录版本号变化触发 CI 打包发布,不与应用发版绑定 | 15 | —(官方仓库、CI 与 Pages 站) |
| D413 | TV 装插件两条路:TV 自带市场页(遥控器浏览,搜索用系统输入法/手机扫码输入 D315,只显示支持 TV 的插件);… | 14 | —(插件页与市场 UI) |
| D414 | 不做从影视仓 / FongMi / TVBox 的数据迁移;这些用户贴订阅地址即可(D45) | 01 | —(范围/原则,无接口) |
| D415 | 2.0.0 发版说明:GitHub Release 详细说明(新插件系统、TVBox 兼容、直播、改成插件的功能及怎么装… | 15 19 | —(官方仓库、CI 与 Pages 站) |
| D416 | 插件页空状态:一句话 + 一个去处按钮(「还没有安装插件 [去市场]」「没找到「xx」[清除搜索]」),不配插画、不写客… | 07 14 | plugin-sdk.d.ts |
| D417 | 插件数据(KV/文件/密钥区)全局一份,宿主不按 Emby 账号隔离;需要区分的插件用 servers.current(… | 05 13 | plugin-sdk.d.ts |
| D418 | 桌面命令面板默认快捷键 Ctrl+Shift+P,可在快捷键设置里改 | 12 | —(宿主系统集成行为) |
| D419 | 命令面板不输 / 时是全局搜索(搜当前服务器的片),输 / 才是命令;与手机搜索页行为一致 | 12 | —(宿主系统集成行为) |
| D420 | 开发者模式:关于页连点版本号 7 次打开;打开后设置里出现「开发者」页(加载本地插件目录 D42、接受推送 D291、主… | 16 20 | —(工具链与文档) |
| D421 | .lpplugin 解压宿主强制校验:拒绝含 ../ 或绝对路径的条目、解压总大小超 2GB 或压缩比超 100 倍的包… | 03 04 15 19 20 | —(宿主强制的安全规则) |
| D422 | 数据源海报与 Emby 图片共用一个磁盘缓存总上限(现有上限与最久未用淘汰),设置里可调上限、一键清理 | 02 | plugin-sdk.d.ts |
| D423 | 首次启动不提 TVBox/插件:首登闸口照旧只有 Emby;添加服务器页里只有装了数据源插件时才出现对应选项(D45) | 08 | —(宿主数据源页面行为) |
| D424 | TV 插件页返回时宿主自动恢复上次焦点(按组件 key 记,配合页面保活 D133);插件可覆盖(D31) | 07 | plugin-sdk.d.ts |
| D425 | TV 插件页宿主默认加安全边距(与官方 TV 页同一套),全屏类页面可声明 edgeToEdge 铺满 | 07 | plugin-sdk.d.ts |
| D426 | 手机插件页默认避让刘海/挖孔/手势条,可声明铺满;铺满时 useViewport() 给出 insets 数值让插件自己… | 07 | plugin-sdk.d.ts ui-protocol.d.ts |
| D427 | 插件可以改应用语言、字体缩放等全局辅助设置(归入 D93「应用设置」可写部分) | 05 | plugin-sdk.d.ts |
| D428 | 系统「减少动态效果」开启时:声明式 transition(D88)与主题关键帧(D213)由原生端直接跳到终态;Canv… | 05 07 | plugin-sdk.d.ts |
| D429 | 聚合视界里,允许聚合的数据源只显示继续观看;没在看的源不占位置(几十个源全列会淹没 Emby) | 08 | —(宿主数据源页面行为) |
| D430 | 全局「观看历史」按时间混排,卡片带来源角标,顶部可按来源筛选 | 08 | —(宿主数据源页面行为) |
| D431 | 同一部片因换源在多个源都有记录时,历史里合并成一条,取进度最远的那条(A 源 10%、B 源 30% → 显示 B 源 … | 08 | —(宿主数据源页面行为) |
| D432 | 弹幕时间偏移按弹幕源分别调(弹幕源列表里每源一个 ± 秒数),按整部剧记住 | 10 | —(宿主对提供者结果的处理) |
| D433 | 数据源 play() 自带的弹幕默认开,作为这一集的一个弹幕源与其它源合并,可在弹幕源列表里关 | 08 10 | plugin-sdk.d.ts |
| D434 | 弹幕源匹配失败/超时/限流时播放页 Toast 提示一次(文案按 D253 错误类型);详细状态在弹幕源列表里 | 10 | —(宿主对提供者结果的处理) |
| D435 | 评分是「带来源名」的列表字段,各家并排显示(豆瓣 / IMDb / TMDB / Bangumi…,带小图标,可点跳到对… | 08 10 | plugin-sdk.d.ts |
| D436 | 元数据插件可按 D74 的覆盖模式替换海报/背景图(只在本地显示);只在详情页生效(D192),海报墙仍是 Emby 原… | 10 | plugin-sdk.d.ts |
| D437 | 插件详情显示启动耗时,超 500ms 标黄;不拦截 | 04 14 20 | —(宿主生命周期行为) |
| D438 | 内存看门狗(D141)销毁插件运行时时 Toast 告知「xx 插件内存占用过高,已暂停」;下次用到自动重建,带界面的区… | 02 | —(宿主架构内部实现) |
| D439 | 启动即加载的插件并行加载,首屏不等;例外:接管首页的插件首页等它(先显示骨架屏 D271),超 3 秒本次回退官方首页 | 04 20 | —(宿主生命周期行为) |
| D440 | TVBox 字段取舍:changeable 忽略(只看 searchable,D235);style 忽略(海报比例由插… | 17 | —(官方插件内部实现) |
| D441 | 壁纸是独立贡献点(接管位),可叠在任意主题上:主题自带壁纸(D212)是默认,壁纸插件可覆盖;切换壁纸不用重启(主题才要… | 04 05 06 11 | examples/wallpaper-dynamic/manifest.json examples/wallpaper-dynamic/src/main.ts manifest.schema.json plugin-sdk.d.ts theme-json.schema.json |
| D442 | 壁纸插件能力:动态切换(按时间/每天/随当前页面或正在看的片变)、在线壁纸源、Canvas/着色器程序生成的动态壁纸、视… | 05 11 18 | examples/wallpaper-dynamic/manifest.json examples/wallpaper-dynamic/src/main.ts plugin-sdk.d.ts |
| D443 | 视频壁纸:进播放页 / 应用到后台 / 系统省电时暂停;低分辨率播放,不占播放用的 mpv 实例。各端视频壁纸的实现方式… | 11 | plugin-sdk.d.ts |
| D444 | 不开放相机扫码、语音输入、振动、独立音频播放这类设备能力 | 01 12 | plugin-sdk.d.ts |
| D445 | 官方组件带无障碍属性 a11yLabel,映射到 Avalonia AutomationProperties / Com… | 07 | plugin-sdk.d.ts |
| D446 | 插件更新新增贡献点(新接管位、新声明局域网访问……)时,更新确认里列出「这次新增」;新接管位不自动生效,照 D116 问… | 04 14 | index.schema.json |
| D447 | 插件更新后设置项:同名保留,其余丢弃;不提供迁移钩子(插件要迁移就自己在 KV 里做) | 04 | —(宿主生命周期行为) |
| D448 | 可在插件详情里选任意历史版本安装;装旧版后该插件锁定不自动更新,直到用户解锁 | 04 14 | index.schema.json |
| D449 | 有更新时可「跳过此版本」:红点消失,直到出更新的版本 | 04 14 | —(宿主生命周期行为) |
| D450 | minAppVersion 高于当前应用时:市场显示但按钮变「需要升级 LinPlayer」;已装插件的新版不兼容时不提… | 04 14 | index.schema.json |
| D451 | 热重载 = 整个插件重载:deactivate → 重新执行 main.js → activate,插件页按原参数重开;… | 04 16 | —(宿主生命周期行为) |
| D452 | 开发版插件(D292)出错时,出错区块显示红色错误覆盖层(错误信息 + 映射回 TS 的栈,D81);正式版仍是 D13… | 04 07 16 | —(宿主生命周期行为) |
| D453 | lp create 生成 TS 项目(空白模板,D293);想写 JS 改扩展名即可 | 16 | —(工具链与文档) |
| D454 | 插件 fetch 默认连接 10 秒 + 空闲 30 秒超时,不设整体超时(慢链路拉大文件合法);可传 timeout … | 05 20 | plugin-sdk.d.ts |
| D455 | 重定向默认跟随,可设 redirect: 'manual' 读到 3xx 状态与 Location(很多解析靠它);插件… | 05 | plugin-sdk.d.ts |
| D456 | 插件网络请求永远记最近 200 条(每插件,内存,不落盘),「复制错误详情」(D172)一并带上;开发者模式下调试面板看… | 04 05 20 | plugin-sdk.d.ts |
| D457 | 手势属于播放页不属于 OSD:插件接管 OSD 后官方手势仍在,插件要改走 D194 的手势注册按手势覆盖 | 06 09 12 | —(宿主对贡献点的调度规则) |
| D458 | Android 画中画时插件 OSD 与所有覆盖层全部隐藏,只留系统小窗控件;插件收到进入/退出画中画事件 | 05 09 | plugin-sdk.d.ts |
| D459 | 退出播放由宿主兜底:返回键 / Esc / 遥控器 Back 永远宿主处理(D84),插件 OSD 不强制放退出按钮 | 07 09 | —(壳/渲染器行为) |
| D460 | 数据源自动下一集:同线路按选集格顺序(不受倒序显示 D332 影响),跟随 Emby 的连播设置;最后一集就停;插件接了… | 08 09 | —(宿主数据源页面行为) |
| D461 | 集名统一显示成「第 N 集」(宿主从源给的名字解析序号);解析不出序号的(「正片」「高清」「花絮」)与日期型集名(综艺「… | 08 | plugin-sdk.d.ts |
| D462 | 简介由宿主统一去 HTML 标签转纯文本(保留换行);去广告尾巴由 TVBox 插件按规则做 | 08 17 | plugin-sdk.d.ts |
| D463 | 只有一集(电影「正片」)的数据源详情页不显示选集格,只有播放按钮,线路标签照留;与 Emby 电影详情一致 | 08 | plugin-sdk.d.ts |
| D464 | 线路间集数不一致时,切线路按集序号对应;目标线路没有这一集就提示「xx 线路没有第 N 集」并留在原线路,不按下标硬对 | 08 | plugin-sdk.d.ts |
| D465 | EPG 刷新:每次启动 + 每 6 小时,缓存 3 天;只在应用运行且打开过直播时拉(EPG 预拉取不属于后台用途,D5… | 17 20 | —(官方插件内部实现) |
| D466 | 台标:m3u tvg-logo > EPG icon > 文字台标(频道名前两字生成) | 17 | —(官方插件内部实现) |
| D467 | 换台时显示信息条 3 秒:台标 + 频道号 + 频道名 + 当前/下一个节目 + 源 1/2/3,3 秒淡出;数字键输频… | 17 20 | —(官方插件内部实现) |
| D468 | 直播源支持频道级请求头:m3u 的 http-user-agent / http-referrer 与 #EXTVLCO… | 17 | plugin-sdk.d.ts |
| D469 | 直播频道管理:隐藏分组/频道、拖动自定义排序、频道搜索(TV 用输入法/扫码 D315)、自定义频道号(数字键直达) | 17 | —(官方插件内部实现) |
| D470 | 同名合并纠错:归一化保留清晰度后缀(4K/8K/高清 不合并)+ 频道长按「拆开此频道的源」+ 设置里合并总开关 | 17 | —(官方插件内部实现) |
| D471 | 直播页返回键:有列表先关列表;再按提示「再按一次退出」,两秒内再按才退出 | 17 20 | —(官方插件内部实现) |
| D472 | 开放 Emby 只读代发 emby.request(路径, 参数):只允许 GET,宿主带 token 发、插件看不到 … | 03 05 | plugin-sdk.d.ts |
| D473 | 插件可注册虚拟媒体库(「豆瓣想看」「我的片单」):返回条目列表(统一结构,可混 Emby 与数据源),官方媒体库页画;在… | 06 | manifest.schema.json plugin-sdk.d.ts |
| D474 | 插件可给条目卡片加角标(「豆瓣 8.9」「已在 Trakt 看过」):角标钩子同步返回、只用已缓存数据,超 300ms … | 01 06 20 | manifest.schema.json plugin-sdk.d.ts |
| D475 | Pages 站不做作者主页(详情页已有「同作者其他插件」,D402) | 01 15 | —(范围/原则,无接口) |
| D476 | Pages 站搜索:构建时生成索引、前端即时搜(名称/描述/作者/拼音首字母),配合网格重排动画(D398);文档搜索用… | 15 | —(官方仓库、CI 与 Pages 站) |
| D477 | 每个插件构建时生成一张分享预览图(图标 + 名称 + 一句描述 + 下载量,站点同风格)用作 og:image | 15 | —(官方仓库、CI 与 Pages 站) |
| D478 | Pages 站提供 RSS/Atom:「新上架」「最近更新」两个 feed,构建时生成 | 15 | —(官方仓库、CI 与 Pages 站) |
| D479 | Pages 插件墙筛选:分类 + 平台(Windows / Linux / Android 手机 / Android T… | 15 | —(官方仓库、CI 与 Pages 站) |
| D480 | Pages 顶部导航:插件 / 文档 / 上架指南 / 下载 LinPlayer + 语言切换 + 明暗切换;页脚:主仓… | 15 | —(官方仓库、CI 与 Pages 站) |
| D481 | 应用内插件详情渲染 README:与 Pages 同一份由官方仓库 CI 生成的静态文件,index 里带其地址,应用按… | 04 14 | index.schema.json |
| D482 | 官方仓库 CI 每 6 小时刷新 star / 下载量 / README 并重建 Pages;合并 PR 时立即重建 | 15 20 | —(官方仓库、CI 与 Pages 站) |
| D483 | 上架/更新 PR 的 CI 校验:manifest 合法 + 版本递增 + 作者名(D265 D294)、包能安装加载(… | 04 15 | index.schema.json manifest.schema.json |
| D484 | lp 另加两个命令:lp check(本地跑一遍与官方 CI 相同的校验,D483)、lp submit(生成上架 PR… | 04 15 16 | —(宿主生命周期行为) |
| D485 | 开发者文档尽量详细,至少四部分:快速开始(10 分钟从 lp create 到手机上看到插件)、扩展点指南(每个扩展点一… | 16 | —(工具链与文档) |
| D486 | 文档每个示例有「在 LinPlayer 中安装」按钮:示例打成 .lpplugin 放站上,走安装深链(D154),按本… | 16 | —(工具链与文档) |
| D487 | 宿主 API player.getSubtitleText(轨道):宿主带 token 拉取字幕轨全文(含 Emby 内… | 03 09 17 | plugin-sdk.d.ts |
| D488 | Whisper 本地转写保留:whisper / ffmpeg 做成宿主下载组件(D380),宿主提供 transcri… | 03 09 17 | plugin-sdk.d.ts |
| D489 | 字幕翻译插件引擎:OpenAI 兼容接口、Anthropic 接口、百度 / 腾讯翻译(用户自己的 key)、免费机翻;… | 17 | —(官方插件内部实现) |
| D490 | 插件密钥区随设置页备份一起导出,不另设密码:与现有备份的「带账号」那一档同进同出(core/config/backup.… | 02 03 05 13 | plugin-sdk.d.ts |
| D491 | 插件可在 manifest 声明「遥控器按钮」(图标 + 名字 + 命令),手机扫码遥控页多一排,点了触发插件命令(如直… | 02 06 12 17 | manifest.schema.json |
| D492 | 插件命令可外部调用:深链 linplayer://cmd/<插件>/<命令>?参数,只限 manifest 声明「可外部… | 06 12 | manifest.schema.json |
| D493 | 多个 m3u8 过滤器串联,顺序由用户排序(同 D280 列表);每个拿上一个的输出,单个出错或超时就跳过它(D281) | 06 20 | manifest.schema.json plugin-sdk.d.ts |
| D494 | 过滤器链删掉的分片超过总时长 30% 就放弃过滤、用原始 m3u8 播,Toast「去广告规则可能误删,已关闭本集过滤」 | 06 20 | plugin-sdk.d.ts |
| D495 | 同一频道多个 EPG 源都有数据时按优先级取第一个有数据的:用户手填 > m3u 头 x-tvg-url > TVBox… | 10 17 | plugin-sdk.d.ts |
| D496 | WebView API 另给:自定义 UA / 请求头、按规则拦截/屏蔽请求(广告域名、图片,加快嗅探)、页面加载前注入… | 05 | plugin-sdk.d.ts |
| D497 | 隐藏 WebView 全局最多 3 个,超出排队;排队时间计入调用超时 | 02 05 20 | plugin-sdk.d.ts |
| D498 | 插件代理路由是完整 HTTP:handler(req) 返回 Response,可流式(直接转发 fetch 的 bod… | 02 03 05 | plugin-sdk.d.ts |
| D499 | 代理路由地址由宿主生成:proxy.url(路径) = 本机地址 + 插件路径 + 随机会话 token,防局域网其它机… | 02 03 05 | plugin-sdk.d.ts |
| D500 | 桌面插件子窗口(D220)能力:置顶 / 无边框 / 透明背景、鼠标穿透(只显示不挡点击)、记住位置与大小;不做多显示器… | 06 07 12 | manifest.schema.json plugin-sdk.d.ts |
| D501 | 插件可改桌面主窗口:托盘右键菜单加项、任务栏进度/角标、窗口标题 | 05 06 12 | manifest.schema.json plugin-sdk.d.ts |
| D502 | 后台形态两种:① 定时唤醒任务:manifest 声明,最短间隔 15 分钟(Android WorkManager 下… | 05 06 12 20 | manifest.schema.json plugin-sdk.d.ts |
| D503 | 后台开关:设置里「允许后台运行」应用级总开关(默认开)+ 插件详情里每插件开关;Android 第一次需要时引导关闭电池… | 05 12 14 | —(运行时内部行为) |
| D504 | 后台能力只给这些用途:刷新桌面小组件 / TV 首页推荐行、追更/开播提醒通知、下载后端轮询(D189)。订阅与 EPG… | 05 12 17 | manifest.schema.json plugin-sdk.d.ts |
| D505 | Android 系统入口:长按图标快捷方式(触发插件命令 D492)、通知带动作按钮、桌面小组件、TV 首页推荐行(频道… | 05 06 12 17 | manifest.schema.json plugin-sdk.d.ts |
| D506 | 桌面小组件用官方模板(海报行 / 列表 / 单卡片三种),插件只填数据;Android Widget 只能用受限的 Re… | 05 06 12 | manifest.schema.json plugin-sdk.d.ts |
| D507 | 桌面点关闭窗口且有插件后台任务时弹窗:「退到后台 / 完全退出」+「记住我的选择」(持久化,设置里可改);没有后台任务的… | 05 12 | —(运行时内部行为) |
| D508 | 后台任务可在 manifest 声明运行条件(仅 Wi-Fi / 仅充电),宿主交给系统调度器约束;默认不限 | 05 12 | manifest.schema.json |
| D509 | 定时唤醒单次最多 60 秒墙钟,超时杀掉;连续超时计入 D53 自动禁用该插件的后台任务(常驻服务不受此限) | 02 05 12 20 | plugin-sdk.d.ts |
| D510 | Android TV 启动器「继续观看」(Watch Next)宿主自己写 Emby 与数据源的在看,播放进度变化时更新… | 08 12 18 | plugin-sdk.d.ts |
| D511 | 手机锁屏/通知栏媒体控制卡片:插件可加 1~2 个自定义动作(「跳过片头」「下一频道」);播放/暂停/进度仍官方 | 06 09 12 20 | manifest.schema.json |
| D512 | 一个插件包的贡献点可以任意组合(如「动漫模式」= 主题 + 首页接管 + 壁纸);但带主题时仍一包一端(D210 D21… | 11 | manifest.schema.json |
| D513 | 同一插件的多个接管位启用时一个弹窗列出全部(「xx 要替换:主题、首页、壁纸」),默认全勾、可逐项取消;合并 D116 … | 04 06 | —(宿主生命周期行为) |
| D514 | 宿主 API 定义源 = 手写 .d.ts(D146 的单一定义文件),注释直接成文档(D392);生成器用 TypeS… | 16 19 | plugin-sdk.d.ts |
| D515 | 插件列表钩子(D278)先跑,宿主媒体库屏蔽最后:用户屏蔽的东西插件插不回来;屏蔽规则同样作用于插件插入的条目 | 02 06 | plugin-sdk.d.ts |
| D516 | 数据源条目可屏蔽(分类、关键词),复用媒体库屏蔽与集中解除列表 | 02 08 | —(宿主架构内部实现) |
| D517 | 2.0.0 之后的官方插件路线:去广告(通用规则)、豆瓣元数据/评分、在线字幕(字幕库类)、B 站弹幕源、官方主题(各端… | 17 | —(官方插件内部实现) |
| D518 | 官方插件开源并接受 PR(维护者审合) | 15 | —(官方仓库、CI 与 Pages 站) |
| D519 | 许可证:官方插件 AGPL-3.0(与主仓库一致);SDK / CLI / 示例 / 文档 MIT,第三方插件许可证随作… | 15 | —(官方仓库、CI 与 Pages 站) |
| D520 | 命令可声明参数,插件提供补全(输入 /直播  后插件返回频道候选);补全钩子同样 300ms 预算(D281) | 01 06 12 20 | manifest.schema.json |
| D521 | 命令名冲突时都列出,后面标插件名;命令内部 id 带插件前缀 | 06 12 | manifest.schema.json |
| D522 | 主题可改粒度(D21 细化):字号刻度与密度(紧凑/宽松)、海报尺寸与网格列数、侧栏/底栏样式与位置(如桌面侧栏改顶栏)… | 11 | theme-json.schema.json |
| D523 | 应用内免责提示三处(各一次性,措辞同 D408 的免责口吻):首次打开市场(插件由第三方开发,官方不对内容负责)、添加第… | 14 17 | —(插件页与市场 UI) |
| D524 | 同一源返回的重名条目(同名同年不同 id)原样显示(常是国语/粤语/完整版等不同版本) | 08 | —(宿主数据源页面行为) |
| D525 | Emby 详情页/播放页也有换源按钮(D232):Emby 的片可换到数据源看(如 Emby 还没更新到最新集)。这是跳… | 08 | —(宿主数据源页面行为) |
| D526 | API 废弃直接改,不设保留期,作者跟进(维持 D36);变更写进 SDK 的 CHANGELOG | 01 04 16 | plugin-sdk.d.ts |
| D527 | 插件启用状态、接管位选择不跨设备同步(TV 上不想开的插件不被手机带跑) | 06 11 13 | —(宿主对贡献点的调度规则) |
| D528 | 设置搜索与命令面板都能搜到插件 manifest 声明的设置项(按标题/说明),结果标来源插件,点了跳到对应设置 | 12 | —(宿主系统集成行为) |
| D529 | 桌面插件页键盘操作由官方组件自带,与 TV 焦点同一套:可点组件自动进 Tab 顺序、回车触发、方向键在焦点组内移动(复… | 07 | plugin-sdk.d.ts |
| D530 | JS 源的 drpy 引擎用配置指定的(api 指向的 drpy2.min.js 等),按 md5 缓存(同 D352)… | 17 | —(官方插件内部实现) |
| D531 | drpy 依赖库(cheerio / crypto-js / jsencrypt / gbk / 模板)插件内置,esb… | 17 | —(官方插件内部实现) |
| D532 | 支持 TVBox type 4(T4 远程 HTTP 爬虫接口):按约定参数请求 HTTP 接口拿 JSON | 17 | —(官方插件内部实现) |
| D533 | 源的 ext 是远程地址时拉取并按订阅周期缓存,拉不到用上次缓存,随订阅刷新一起更新 | 17 | —(官方插件内部实现) |
| D534 | 配置里的「代理直播」(proxy://do=live…,靠 jar 解析)走 jar 运行环境 + 兼容入口(D349 … | 05 17 | plugin-sdk.d.ts |
| D535 | 直播频道手动一键检测:并发探测频道地址,失效的标灰并排到该频道源列表后面;播放时自动切源(D61)照旧。不做后台定期检测 | 17 | —(官方插件内部实现) |
| D536 | 插件可生成图片:Canvas 离屏绘制导出 PNG;保存到相册/图片文件夹(手机存相册,桌面弹保存框 D245);系统分… | 05 07 12 | plugin-sdk.d.ts |
| D537 | 片头片尾提供者扩展点(对 Emby 片也生效,如社区片头库、音频指纹识别):优先级 Emby 自带标记 > 用户手设(D… | 01 08 10 | plugin-sdk.d.ts |
| D538 | 进度条预览图提供者对 Emby 片也生效(扩展 D163):宿主提供抽帧 API(用 ffmpeg 组件 D380,宿主… | 01 03 09 10 | plugin-sdk.d.ts |
| D539 | 插件改动触发的重启(D169 D170)之后按启动页设置(D130)进入,不恢复重启前的页面 | 04 | —(宿主生命周期行为) |
| D540 | 自动更新(D49 开启时):启动时检查并下载、安静暂存,下次启动生效,不弹窗;插件页显示「已更新,下次启动生效」 | 04 | —(宿主生命周期行为) |
| D541 | 插件合集:用户可把一组已装插件导出为合集(小 JSON:插件 id + 来源),生成深链/二维码分享;对方打开一次确认装… | 12 13 | —(宿主系统集成行为) |
| D542 | 插件详情可「导出设置」为文件/二维码分享给别人,导入到同一插件;密钥区永不进分享 | 13 | —(宿主存储/同步/备份行为) |
| D543 | 第 ② 阶段(UI 渲染器)验收:调试面板插件在桌面跑通;三端同一组示例页(布局 / 列表 / 输入 / Canvas … | 07 17 19 20 | —(壳/渲染器行为) |
| D544 | 第 ④ 阶段(直播)验收:公开 IPTV 源(本地测,地址不进仓库)能播、能换台;XMLTV 节目单显示正确;回看/时移… | 19 | —(实施计划与验收) |
| D545 | 第 ③ 阶段验收:老功能对等(对照删除前的提交逐项勾:同步、字幕翻译、Whisper;排行榜、追剧日历在 ① 期间恢复时… | 18 19 | —(宿主内置功能) |
| D546 | 第 ⑤ 阶段验收:各端一套官方示范主题;主题出错回退(D372)先红后绿测试;壁纸与视频壁纸(D441~D443)跑通 | 19 | —(实施计划与验收) |
| D547 | 插件通知每插件每小时最多 5 条,超出折叠成一条「xx 还有 N 条通知」(不丢) | 05 12 20 | plugin-sdk.d.ts |
| D548 | 不做应用内通知中心(系统通知栏就是) | 01 12 | —(范围/原则,无接口) |
| D549 | 入口小图标(侧栏 / 命令面板 / 快捷方式 / 遥控按钮)要求单色 SVG,宿主按主题着色,跟随深浅色;商店大图标(D… | 04 | manifest.schema.json plugin-sdk.d.ts |
| D550 | 推翻 D228:老用户「已解锁沿用」做不到 —— 旧解锁记录存在 Rust 栈 Tauri WebView 的 loca… | 18 | —(宿主内置功能) |
| D551 | 付费门锁整页(照 rust-final:ui/desktop/pages/CalendarPage.tsx):未解锁进追… | 18 | —(宿主内置功能) |
| D552 | goja 遇回溯类正则落到 regexp2,而 regexp2 默认不超时、匹配中途不查 goja 的中断标志,一条坏规… | 02 19 | —(宿主架构内部实现) |
| D553 | 单次原生大操作(超大数组 join / 字符串 repeat)中途不查中断标志,BudgetData/BudgetHoo… | 02 16 19 20 | —(宿主架构内部实现) |
| D554 | TV 不做「源内搜索框」:遥控器敲字本来就慢,数据源首页再放一个框 = 同一件事两套输入流程。TV 上这件事由搜索页承担… | 08 19 | —(宿主数据源页面行为) |
| D555 | .d.ts 声明的 hooks 必须全部挂上(D514 只管了命名空间与组件,hooks 一直在门禁外):gen.mjs… | 07 16 | —(壳/渲染器行为) |
| D556 | 主题 token 名清单定版(20.4):样式里的 'token:名字'、useTheme().token()、手机/T… | 07 20 | —(壳/渲染器行为) |
| D557 | 澄清 D136 的「区块级」= surface 级,不是子树级。2026-09-20 的复核把它读成「只崩那一块子树」并… | 07 | —(壳/渲染器行为) |
| D558 | 壳向核心层报运行环境(plugin.setEnv:主题明暗 + token 表 + 系统「减少动态效果」),与 plug… | 07 | —(壳/渲染器行为) |
| D559 | linplayer/devtools 插件不限平台,但里面只有面板那一页限桌面(D367 原意):手机/TV 打开它显示… | 17 | —(官方插件内部实现) |
| D560 | 推翻 D375:不做「下载 GeckoView 动态加载」。GeckoView 主体是原生 libxul.so + 一整… | 05 19 | —(运行时内部行为) |
| D561 | D268 的前提作废:桌面视频不是独立子窗口(那是已删除的 Rust + Tauri 栈),现在是视觉树里的 OpenG… | 07 11 19 | —(壳/渲染器行为) |
| D562 | <ProgressBar> 补一个可选的 value(0~1):不给就绑当前播放进度(D161 原意,原生直接读 mpv… | 07 | —(壳/渲染器行为) |
| D563 | 插件要能收播放器按键:加 player.onKey(cb),回调回 true = 这一下我接走了。没有它的话 SPEC … | 09 | examples/player-panel/src/main.tsx plugin-sdk.d.ts |
| D564 | D443 的 spike 出结论:视频壁纸安卓做、桌面 2.0.0 不做。安卓用已经在依赖里的 ExoPlayer(和 … | 11 19 | —(主题加载与宿主设置) |
| D565 | 红线的唯一例外:面向用户的自有公开资产豁免(负责人 2026-09-21 裁决,结清 docs/lessons/red-… | 15 | —(官方仓库、CI 与 Pages 站) |
| D566 | 官方插件仓库整仓 AGPL-3.0,推翻 D519 的「SDK / CLI / 示例 / 文档 MIT」分法(负责人 2… | 15 | —(官方仓库、CI 与 Pages 站) |
| D567 | 官方市场地址走 Pages 站,不走 raw / 单独托管:plugin-repo/site/src/pages/reg… | 15 | —(官方仓库、CI 与 Pages 站) |
| D568 | 官方插件的 minAppVersion 从 1.1.0 抬到 2.0.0。它们用的是 2.0.0 的插件系统,1.1.x… | 14 | —(插件页与市场 UI) |
| D569 | Trakt / Bangumi 的账号连接回桌面与手机设置页。6b290d80 把同步整体移出宿主时,连账号连接的入口一… | 18 | —(宿主内置功能) |
| D570 | 大版本(x.0.0)的发布说明必须有人写的那一段:scripts/release-notes.sh 把 docs/plu… | 19 | —(实施计划与验收) |
| D571 | drpy 上游地址写进脚本,不走 Secret(负责人 2026-09-21 裁决)。原来它放在被忽略的 scripts… | 17 | —(官方插件内部实现) |
| D572 | 全部官方插件共用一条 Release(tag plugins),推翻「一个插件一条 Release」的发法(负责人 20… | 15 | —(官方仓库、CI 与 Pages 站) |
| D573 | 不是标准 TVBox 形状的配置,由插件认回来,而不是让用户自己改文件(用户 2026-09-21 报障:「插件不支持导… | 17 | —(官方插件内部实现) |
| D574 | 「导入一个文件」由壳读文件填进输入框,不新开贡献点。插件读不到本地文件是故意的(rt/system.go 拦 file:… | 12 | —(宿主系统集成行为) |
| D575 | 苹果 CMS 的顶级分类要展开成子分类再查。class 是一张平表,顶级项(type_pid: 0:电影 / 电视剧 /… | 17 | —(官方插件内部实现) |
| D576 | 「云播」线路是个网页,要先把地址抠出来再播。资源站的第二条线路常长成 https://<云播站>/play/<一串 id… | 17 | —(官方插件内部实现) |
| D577 | 信号栈探针的对照组给三次机会(scripts/sigprobe-linux.sh)。它要证明「关掉修复必须崩」才算有牙,… | 19 | —(实施计划与验收) |
| D578 | 订阅分组的折叠记「用户的选择」,不是「当前该不该开」。原来三个壳写法一样:open = 展开名单里有它 // 这组里有正… | 08 | —(宿主数据源页面行为) |
| D579 | 「首页栏目」只列 Emby 服务器。那个开关管的是合集栏,而数据源(TVBox 这类采集站)没有合集、也没有首页的概念 … | 08 | —(宿主数据源页面行为) |
| D580 | 插件的侧栏入口(contributes.sidebar)三个壳都要画。核心层的 plugin.sidebar 从阶段 ②… | 06 | —(宿主对贡献点的调度规则) |
| D581 | UHD 助手合成一个官方插件(linplayer/uhd,流量 / 求片 / 测速三合一),旧栈那三个独立插件作废。三块… | 17 | —(官方插件内部实现) |
| D582 | 字形是否在 LinIcons 里,推之前本机就要红:gen-icon-font.py --check 从只挂在 pack… | 19 | —(实施计划与验收) |
| D583 | 首页栏目(homeSections)接通:核心层加 plugin.homeSections(取表)与 plugin.ho… | 06 | —(宿主对贡献点的调度规则) |
| D584 | TV 也画插件的首页栏目,门禁按三个壳算。D583 只做了桌面和手机,TV 那次没做 —— 而 check-plugin… | 06 | —(宿主对贡献点的调度规则) |
| D585 | 宿主没接的贡献点要当场说出来,不许装作接了。contributes 有 35 个键,宿主这一版真接了的只有 11 个,另… | 06 | —(宿主对贡献点的调度规则) |
| D586 | 整页接管接通最后一段:核心层加 plugin.pageTakeovers(只列用户选中的、且插件还启用着的),三个壳画官… | 06 | —(宿主对贡献点的调度规则) |
| D587 | 搜索页快捷动作(D242)接通,连同「点了要能跑」的另一半。核心层 plugin.searchActions(把用户输的… | 06 | —(宿主对贡献点的调度规则) |
